
博客补充嵌入式两方向，freertos+mcu，


内核内存管理再探，e820，memblock，buddy，slub，   syscall，brk，mmap，ptmalloc





ai算力引擎优化和部署原理，vllm，sglang，llama.cpp，学习了这个我才知道那些便宜的硬件值不值得买
后训练量化，gptq，awq，



sigmoid
softmax
ReLU
GeLU
SwiGLU
tanh

多头自注意力层
前馈全连接层
残差层 和 归一化层

ppo dpo grpo

blender mcp

obs studio

lxc 容器

io操作优先走声明式逻辑，如果是动态生成的io，与其直接开始io操作，不如返回一个代表io操作的策略或者io函数，让容器来代为执行。或者触发一个 event，让事件循环来捕获

我们其实无需容器，只要我们能够手动管理得了状态迁移和纯函数规范即可，但是，实际上很多人以为自己能做到，其实做不到。
纯函数编程中如果遇到函数参数过多怎么办，首先是功能拆分，函数太大了。参数封装成一个对象，
程序包含数据结构和函数计算（状态转移方法），程序的复杂来自于空间复杂性，和时间复杂性
基于搜索的依赖获取和依赖管理，基于模式匹配的搜索方式

状态的生命周期管理，流程图分析法；生命周期钩子互相调用时应当谨慎。面向对象的状态封装思想。如果你的一个资源结构体需要在深层多级函数中层层传递，最后只为了拿其中一个状态，那么说明你的资源划分没划分对。
逻辑抽象具体化，两个结构体之间具有某种关系，可以使用一个第三方结构体来表征它们的关系。考虑抽象多个结构体的关系时，可以通过结构体各自的生命周期来分析关系。
父结构体的生命周期和子结构体的生命周期


解释面向对象中的方法，其本质是依赖绑定和隐式依赖声明

多级散布链表，用于解决空间总是有限的局限性

mlir ，tvm，triton，xla，ai 发展还得学编译原理

linux macvlan 模块

