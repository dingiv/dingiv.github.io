可读性与可维护性（可写性，可改性），可读不等于可维护

可靠性与可用性，可靠代表不能错，可用代表你可以错，但是你必须马上纠正

容器技术增加正向容器和反向容器，讲解安全计算

流式存取和块式存取

矩阵和图遍历算法，处理当前节点，试探下一个节点来决定是否进入下一个节点，进入下一个节点

模型集群 pd 分离

bmc 主板管理器

rn 和 flutter 技术对比，diff 树

研究 4G 模块时会发现，它们插在普通的转接板上几乎“一插就准”，原因如下：

总线协议简单：绝大多数 4G 模块走的是 USB 2.0/3.0 协议，而不是 RM520 这种复杂的 PCIe + MHI 协议。USB 协议自带完善的容错机制和热插拔管理，不会像 PCIe 那样对时序（Reset）要求到毫秒级。

功耗控制：4G 模块峰值电流一般在 1A 左右，普通的 PCIe 转接板供电绰绰有余。而 5G 模块在搜网时巨大的瞬时功耗会直接拉跨劣质转接板的电压。

驱动成熟度：4G 模块的 option 或 qmi_wwan 驱动在 Linux 内核里已经躺了十几年了，稳定性极高。

局域网通信技术，ethernet，pcie，usb，蓝牙，2.4g/5g, infiniband, fiber channel 光纤，thunder blot，can 网络 control ler area network


矩阵和图遍历算法，处理当前节点，试探下一个节点来决定是否进入下一个节点，进入下一个节点

模型集群 pd 分离

api gateway 的作用 协议转换 http 2 rpc

sdl2 图形

容器的本质是一个被蒙蔽了双眼的进程，如果一个容器里面有多个进程那么就是一组被同一片树叶蒙蔽了双眼的进程

proc_fs，proc_create

讲讲 内核编译参数和内核启动命令

数据库
基本使用，访问层，执行层，存储层，

可观测性，elk，prometheus，监控，日志

proc 目录，系统的瞬时快照，添加常见目录


openai rpc 库，

acid

$（）的错误在 local 赋值之后不会被捕获，但是local 声明不和赋值在一起可以

并发锁的思想就是访问自肃

linux 进程树与继承性

session 管理 三个 id，user id，session id，task id，
动态压缩，如果当前的上下文过过长，则需要使用语言模型对上下文进行总结压缩

qemu 调试内核开发，linux 错误捕获机制，内核开发调试手段需要进一步强化，需要专门做一个用于调试内核的虚拟机模拟器

算法：二分法的循环条件和状态转移比较特殊，

left <= right 配合  mid = left + 1，三个分支

linux 进程树拓扑结构， fork 树

efi 是一个微型操作系统，

linux 性能调优，服务器莫名卡顿

内存屏障和并发，什么时候，哪些场景需要内存屏障

内存管理，新增 e820 表

e820 表项，

blog新增手写操作系统的章节

内核并发
原子变量，rcu，per cpu 变量，seqlock，单写者模型，中断和抢占禁用

常用内核命令行，主要学习调试开发内核时使用的

如果说，虚拟机能够在调试内核的时候将物理内存中的值实时显示，那么可调试性会友好很多，还就是监控 cpu 读取了哪些设备的寄存器

x86 物理内存映射兼容性设计
e820 和 memblock 的关系，memblock 子系统是内存早期的管理 api 

/sys 目录和 kset kobject 模块


blog虚拟化模块新增 spdk和dpdk


新增iommu，iommu_group，dma，sendfile，pci，numa，host_bridge 内容讲解

新增需求分析，基础需求，刚需，亮点需求

