import{_ as i,o as e,c as s,ah as l}from"./chunks/framework.BvDvRtye.js";const k=JSON.parse('{"title":"网络实现","description":"","frontmatter":{"title":"网络实现","order":60},"headers":[],"relativePath":"kernel/linux/net/index.md","filePath":"kernel/linux/net/index.md"}'),t={name:"kernel/linux/net/index.md"};function n(r,a,o,p,h,c){return e(),s("div",null,[...a[0]||(a[0]=[l(`<h1 id="网络实现" tabindex="-1">网络实现 <a class="header-anchor" href="#网络实现" aria-label="Permalink to “网络实现”">​</a></h1><p>总结来说，Linux 的网络架构分成了如下层次：</p><ol><li>应用层</li><li>系统调用 socket API，更多内容参考<a href="./../syscall/">系统调用</a></li><li>插口层/套接字层，使用请参考<a href="./../develop/socket">套接字编程</a></li><li>协议层/协议栈，实现<a href="/basic/network/protocol">网络协议</a>各个层</li><li>驱动层，更多内容参考<a href="./../device/">设备管理</a></li><li>物理层</li></ol><h2 id="网络接口层" tabindex="-1">网络接口层 <a class="header-anchor" href="#网络接口层" aria-label="Permalink to “网络接口层”">​</a></h2><p>在 Linux 中，通过 <code>net_device</code> 结构体抽象一个<strong>网络接口</strong>，或者叫<strong>网卡</strong>。这是一个极具误导性的命名，它不是严格的网络设备，或者说远没有看上去那么简单。该定义是一个大杂烩，它直接包揽了二层和三层的所有内容，从数据结构上不再区分二层和三层，而网络的协议和数据收发是通过协议栈来进行，网络设备不关心数据操作，只呈现属性。这一层是 Linux 网络“可塑性”的来源。</p><div class="language-c"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device_ops </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">netdev_ops;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> header_ops </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">header_ops;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> netdev_queue </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">_tx;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    netdev_features_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> gso_partial_features;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> real_num_tx_queues;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> gso_max_size;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> gso_ipv4_max_size;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    u16 gso_max_segs;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    s16 num_tc;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ... more properties</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>它向上提供发送接口（ndo_start_xmit），向下绑定驱动，内部管理 TX/RX 队列，维护统计信息、MTU、特性位；</p><p>从数据路径角度看：协议栈通过 dev_queue_xmit() 将 skb 推入 net_device，驱动通过 NAPI poll 将 skb 提交给协议栈。</p><p>net_device 本质上是“包交换的接口契约”。它不处理协议逻辑，不解析 IP，不关心 TCP 状态机。它只是流量的出入口。</p><h3 id="eth" tabindex="-1">eth <a class="header-anchor" href="#eth" aria-label="Permalink to “eth”">​</a></h3><p>真实物理以太接口。</p><h3 id="tap-虚拟以太网卡" tabindex="-1">TAP 虚拟以太网卡 <a class="header-anchor" href="#tap-虚拟以太网卡" aria-label="Permalink to “TAP 虚拟以太网卡”">​</a></h3><p>TAP 网卡是一个 Linux 内部实现的<strong>用户空间</strong>虚拟以太网接口，同样使用一个 net_device 结构体来表示。尽管真实的物理线路上并没有物理网络接口，但是操作系统可以假装有一个，并将写入这个接口中的数据，发送到其他地方去，如应用层的应用软件，而不是调用真实物理网卡的驱动。这是一个非常强大的设计，这意味着，Linux 的用户可以定义用户空间的&quot;物理&quot;传输链路，这也为虚拟机的网络技术提供了一个简单而可靠的方法。</p><p>由于网络的分层架构，二层的以太网络接口其实和一层的物理链路之间不一定是强耦合的，所以下层的物理链路可以是基于普通的<strong>双绞线</strong>网线，也可以基于<strong>无线信号</strong>、<strong>光信号</strong>。并且，在 Linux 中，由于可以给用户创建一个二层的网络设备 tap，一个 tap 意味着一个虚拟的网络接口，其数据的传输走的是软件处理，因此数据也可以走虚拟通路进行传输。</p><p>TAP 网卡拥有前后端：</p><ul><li>前端是协议栈</li><li>后端是用户空间的自定义的数据传输软件</li></ul><p>我们一般只需要实现 TAP 网卡的后端。当我们创建了一个 TAP 网卡 <code>tap0</code> 之后，在 <code>/dev/net/tap0</code> 路径将会出现我们创建的网卡在 Linux VFS 上的文件抽象。</p><ul><li>当我们<strong>读</strong>该文件的时候，相当于模拟<strong>网卡处理从协议栈发出的数据，并向外界发送数据</strong></li><li>当我们<strong>写</strong>该文件的时候，相当于模拟<strong>网卡接收到从外界发送来的数据，并通过中断通知上层协议栈进行处理</strong></li></ul><p>更进一步的讲，我们需要向 TAP 设备读写和处理<strong>以太帧</strong>。</p><h3 id="tun-虚拟三层网卡" tabindex="-1">TUN 虚拟三层网卡 <a class="header-anchor" href="#tun-虚拟三层网卡" aria-label="Permalink to “TUN 虚拟三层网卡”">​</a></h3><p>TUN 设备与 TAP 设备类似，但它在三层（IP 层）操作，处理的是 IP 数据包而不是以太帧。TUN 设备常用于 VPN 隧道（如 OpenVPN）和 IP 隧道协议（如 IPIP、GRE）。TUN 设备的读写操作处理的是 IP 数据包，没有以太网头部。</p><p>TUN/TAP 设备的存在使得用户空间程序可以完全参与协议栈的数据流动，这是实现虚拟机网络（如 QEMU/KVM）、容器网络（如 Docker、Kubernetes）、网络功能虚拟化（NFV）的基础。TUN/TAP 的设计体现了 Linux 网络的哲学：协议栈是可插拔的。</p><h3 id="bridge-网桥-虚拟交换机" tabindex="-1">Bridge 网桥-虚拟交换机 <a class="header-anchor" href="#bridge-网桥-虚拟交换机" aria-label="Permalink to “Bridge 网桥-虚拟交换机”">​</a></h3><p>网桥是一个虚拟二层网卡，也可以将其看做操作系统内部实现的虚拟交换机，使用软件模拟的交换机，性能较差（交换机转发流量的行为逻辑一般是由硬件直接实现的，因此性能极高），它让通用计算机体现出与交换机类似的行为，是容器网络和虚拟机网络的基础。虽然软件转发性能不如 ASIC 交换机，但借助 XDP、硬件 offload，可以将部分转发逻辑下沉到网卡。</p><p>网桥根据 MAC 地址转发以太帧，维护一个 MAC 地址到端口映射的转发表（FDB，Forwarding Database）。网桥可以连接多个网络接口（物理或虚拟），使这些接口处于同一个二层广播域中。网桥的实现涉及内核的 netfilter 框架，通过在数据包的路径上注册钩子函数来实现转发逻辑。</p><h3 id="vlan-virtual-local-area-network-虚拟局域网" tabindex="-1">VLAN-Virtual Local Area Network 虚拟局域网 <a class="header-anchor" href="#vlan-virtual-local-area-network-虚拟局域网" aria-label="Permalink to “VLAN-Virtual Local Area Network 虚拟局域网”">​</a></h3><p>VLAN 允许在同一个物理网络上创建多个逻辑隔离的网络。它的主要作用是：</p><ul><li>隔离广播域，提高安全性</li><li>灵活管理网络结构</li><li>将一个物理网络划分为多个独立的逻辑网络</li><li>允许一个网卡连接多个网桥（通过 VLAN 子接口）</li><li>减少广播流量，提升网络性能</li></ul><p>VLAN 在 Linux 中通过 802.1Q 实现。</p><p>VLAN 通过 VLAN Tagging 允许一个物理网卡创建多个 VLAN 子接口，每个 VLAN 接口可以加入不同的网桥，从而让一个以太网接口（eth0）能够连接多个网桥。一个物理接口可以派生出多个子接口（eth0.100）。VLAN 让广播域逻辑独立于物理拓扑。这是一种常见的网络管理方式，在虚拟化和云计算环境中非常常见。</p><h3 id="bond" tabindex="-1">Bond <a class="header-anchor" href="#bond" aria-label="Permalink to “Bond”">​</a></h3><p>Bond 是<strong>网络绑定（NIC Bonding）</strong>创建的一个逻辑网络接口，用于将多个物理网卡（NIC）绑定在一起，提高网络的带宽、冗余性和可靠性。它是 Linux 网卡聚合（Link Aggregation）的实现，类似于 LACP（IEEE 802.3ad）。</p><p>Bond 支持多种工作模式：mode-0（balance-rr）轮询策略；mode-1（active-backup）主备策略；mode-4（802.3ad）LACP 聚合；mode-5（balance-tlb）自适应传输负载均衡；mode-6（balance-alb）自适应负载均衡。</p><h3 id="macvlan" tabindex="-1">Macvlan <a class="header-anchor" href="#macvlan" aria-label="Permalink to “Macvlan”">​</a></h3><p>Macvlan 是另一种虚拟网络接口技术，它允许在一个物理接口上创建多个虚拟接口，每个虚拟接口有自己独立的 MAC 地址。Macvlan 适用于容器网络场景，每个容器可以拥有自己的虚拟网卡并直接连接到物理网络。但是，Macvlan 需要一个主设备，主设备</p><p>Macvlan 有几种工作模式：VEPA 模式让流量发往物理交换机并由交换机转发回同一主机上的其他 macvlan 接口；Bridge 模式允许同一物理接口上的 macvlan 接口直接通信；Private 模式隔离同一物理接口上的 macvlan 接口；Passthru 模式允许一个物理接口传递所有 MAC 地址。缺点是：主机和子接口默认不能互通。</p><h2 id="协议栈" tabindex="-1">协议栈 <a class="header-anchor" href="#协议栈" aria-label="Permalink to “协议栈”">​</a></h2><p>Linux 网络协议栈是实现网络层协议的组件，它基于下层的设备抽象层，同时为上层的 socket 层提供协议的抽象机制。协议栈采用分层设计，每层协议通过注册回调函数的方式参与数据包的处理流程。</p><p>所有网络数据在内核中都封装为 struct sk_buff（skb）。</p><p>skb 是一个高度优化的包描述符：</p><p>头部指针分层推进（mac_header / network_header / transport_header）</p><p>支持 clone（零拷贝）</p><p>支持 scatter-gather</p><p>支持 GSO/GRO</p><p>理解 skb 的生命周期，是理解 Linux 网络性能的关键。</p><p>数据路径本质上是 skb 在不同子系统之间移动。</p><h3 id="routing-表-路由表" tabindex="-1">Routing 表-路由表 <a class="header-anchor" href="#routing-表-路由表" aria-label="Permalink to “Routing 表-路由表”">​</a></h3><p>路由表决定数据包的转发路径，存储网络目标和下一跳信息。Linux 支持多张路由表，通过路由策略（RPDB）决定使用哪张路由表。路由查找基于最长前缀匹配原则，路由表可以配置静态路由，也可以通过动态路由协议（如 OSPF、BGP）学习路由。</p><h3 id="arp-表" tabindex="-1">ARP 表 <a class="header-anchor" href="#arp-表" aria-label="Permalink to “ARP 表”">​</a></h3><p>ARP (Address Resolution Protocol) 表将目标主机的 IP 地址映射为目标主机的 MAC 地址，是实现从三层切换到二层的机制。当主机需要发送数据包到同网段的另一个主机时，它首先查询 ARP 表，如果未找到对应的 MAC 地址则广播 ARP 请求。ARP 表项有超时时间，通常为几分钟。</p><h3 id="fdb-表" tabindex="-1">FDB 表 <a class="header-anchor" href="#fdb-表" aria-label="Permalink to “FDB 表”">​</a></h3><p>FDB (Forwarding Database Table) 表将目标主机的 MAC 地址映射为交换机 port 号的表，是类似于交换机的 MAC 表。对于 Linux 网桥来说，FDB 表维护了 MAC 地址到网桥端口的映射关系，用于决定如何转发以太帧。FDB 表可以通过 <code>bridge fdb</code> 命令手动管理。</p><h3 id="netfilter-框架-iptables" tabindex="-1">Netfilter 框架 - iptables <a class="header-anchor" href="#netfilter-框架-iptables" aria-label="Permalink to “Netfilter 框架 - iptables”">​</a></h3><p>Netfilter 是一套 Linux 上的网络过滤框架，它在网络包收发的各个过程中添加钩子，从而拦截和过滤流量，为了网络安全而设计。在这个框架中，使用一个名叫链（Chain）的概念来表达一个钩子，&quot;链&quot;就像是数据包在内核中旅行时要经过的一个个&quot;检查站&quot;，每个检查站（链）都有一堆规则，这些规则决定数据包是放行（ACCEPT）、丢弃（DROP）、修改（NAT）还是其他操作，每个点对应数据包生命周期的不同阶段。</p><p>该框架包含多张表，定义了为数据收发的不同阶段添加钩子，满足多样的网络过滤规则。包括：filter、NAT、mangle、raw、security 等，其中 filter 和 NAT 最常用。</p><h4 id="filter-表-防火墙" tabindex="-1">Filter 表-防火墙 <a class="header-anchor" href="#filter-表-防火墙" aria-label="Permalink to “Filter 表-防火墙”">​</a></h4><p>该表直接决定了是否处理一个数据包，是防火墙的底层基础，包含三个钩子（链、Chain）：</p><ul><li>INPUT：在三层数据包的目标是本机时触发</li><li>FORWARD：在三层数据包的经过本机进行转发时触发</li><li>OUTPUT：在本机发出三层数据的时候触发</li></ul><p>钩子可以指定一些规则，用于匹配数据包的特征，例如可以匹配数据包的源地址、目标地址、协议类型等。当规则被匹配的时候，进行相应的行为：</p><ul><li>ACCEPT：接收该包</li><li>DROP：丢弃该包</li><li>REJECT：明确拒绝该包</li></ul><p>防火墙规则的匹配是按顺序进行的，一旦匹配成功就执行相应的动作并停止后续规则的检查。</p><h4 id="nat-表-网关" tabindex="-1">NAT 表-网关 <a class="header-anchor" href="#nat-表-网关" aria-label="Permalink to “NAT 表-网关”">​</a></h4><p>网关是指当一个 Linux 机器位于多个子网中时，那么它可以作为子网之间交互的通道，这个操作就是 NAT 操作。Linux 一般默认支持 NAT 的功能，但是默认关闭，必须为其配置转发规则（通过 <code>sysctl -w net.ipv4.ip_forward=1</code>），才能使得 NAT 开始工作，然后子网中的其他机器可以通过配置路由表项，告诉访问某个网段的时候使用该机器作为网关。</p><p>NAT 表直接决定了本机的 NAT 行为是否进行，是网关功能的底层实现，包含三个钩子：</p><ul><li>PREROUTING：数据包进入系统后的第一个处理点，用于修改目标地址（DNAT）</li><li>POSTROUTING：数据包离开系统前的最后一个处理点，用于修改源地址（SNAT 或 MASQUERADE）</li><li>OUTPUT：本地主机发出的数据包，用于修改目标地址（DNAT）</li></ul><p>回调：</p><ul><li>DNAT：目标地址修改</li><li>SNAT：源地址修改</li><li>MASQUERADE：掩饰，动态修改数据包的源地址，增强版 SNAT</li><li>REDIRECT：将数据包的目标地址重定向到本地主机的某个端口</li><li>RETURN：自定义链</li><li>ACCEPT</li><li>DROP</li><li>NETMAP</li><li>TPROXY</li></ul><h3 id="traffic-control-qos-quality-of-service" tabindex="-1">Traffic Control-QoS (Quality of Service) <a class="header-anchor" href="#traffic-control-qos-quality-of-service" aria-label="Permalink to “Traffic Control-QoS (Quality of Service)”">​</a></h3><p>流量控制模块，是 Linux 中与协议栈平行的一个网络控制子系统，用来管理网络流量的带宽、延迟和优先级。QoS 的目标是优化网络性能，确保关键流量优先传输，避免拥堵。</p><p>Linux 支持多种队列规则：pfifo_fast 是默认的队列规则；tbf（Token Bucket Filter）令牌桶过滤器，可以限制带宽；htb（Hierarchy Token Bucket）分层令牌桶，支持复杂的层次化带宽分配；fq_codel（Fair Queuing with Controlled Delay）结合了公平队列和主动队列管理，可以减少延迟和队列溢出。</p><h2 id="套接字" tabindex="-1">套接字 <a class="header-anchor" href="#套接字" aria-label="Permalink to “套接字”">​</a></h2><p>传输层面向进程，向进程提供了编程接口 Socket，并且在各大操作系统中均被采用，拥有较为统一的心智模型。其中，TCP 和 UDP 是协议栈的客户端，其 socket 内部会调用协议栈完成基于网络的交互。上层的接口采用 socket 接口进行抽象和定义，与下层无关，并且 socket 接口完全可以选择不走下层的协议栈，例如：unix socket 和 virtual socket。</p><p>Socket 在内核中由 <code>socket</code> 结构体表示，包含协议族、类型、协议、状态、等待队列、发送和接收缓冲区等字段。Socket 的数据传输使用 <code>sk_buff</code> 结构体（简称 skb）来封装数据包，skb 是一个双向链表节点，可以串联成队列，它包含数据包的各个层级的头部信息、数据指针、长度信息等。</p><h2 id="tcp" tabindex="-1">TCP <a class="header-anchor" href="#tcp" aria-label="Permalink to “TCP”">​</a></h2><p>TCP Socket 提供面向连接的、可靠的、字节流传输服务。TCP Socket 的状态机包括 CLOSED、LISTEN、SYN_SENT、SYN_RCVD、ESTABLISHED、FIN_WAIT_1、FIN_WAIT_2、CLOSE_WAIT、CLOSING、LAST_ACK、TIME_WAIT 等状态。</p><p>应用程序通过 <code>listen()</code> 系统调用将 Socket 进入 LISTEN 状态，等待客户端的连接请求；通过 <code>connect()</code> 系统调用主动发起连接；通过 <code>accept()</code> 系统调用接受连接请求，返回一个新的 Socket 用于与客户端通信。</p><p>Linux 为 TCP Socket 提供了丰富的配置选项，可以通过 <code>setsockopt()</code> 系统调用或 <code>/proc/sys/net/ipv4/</code> 下的 sysctl 参数进行调优。重要的参数包括 TCP_KEEPALIVE_TIME、TCP_MAX_SYN_BACKLOG、tcp_slow_start_after_idle 等。</p><h2 id="udp" tabindex="-1">UDP <a class="header-anchor" href="#udp" aria-label="Permalink to “UDP”">​</a></h2><p>UDP Socket 提供无连接的、不可靠的数据报传输服务。UDP Socket 不需要建立连接，每个数据报独立发送，可能沿不同的路径到达，可能乱序、重复或丢失。UDP 的简单性使得它的开销很小，适合对延迟敏感但可以容忍数据丢失的应用。</p><p>UDP Socket 的发送和接收相对简单：发送时，数据从用户空间复制到内核，添加 UDP 头部和 IP 头部后直接发送；接收时，数据到达后经过协议栈处理，最终复制到接收缓冲区。应用程序需要自行实现可靠性机制（如重传、确认、顺序控制）。</p><h2 id="unix" tabindex="-1">Unix <a class="header-anchor" href="#unix" aria-label="Permalink to “Unix”">​</a></h2><p>Unix Domain Socket（AF_UNIX）是一种特殊的 Socket，它用于同一主机上的进程间通信（IPC），不经过网络协议栈。Unix Socket 使用文件系统路径作为地址，数据完全在内核中传递，不涉及网络接口，因此性能远高于网络 Socket。</p><p>Unix Socket 支持流式和数据报两种类型，还可以传递文件描述符（通过 SCM_RIGHTS 辅助消息）。Unix Socket 在许多场景中被使用：X Window 客户端与服务器的通信、Docker 守护进程与客户端的通信、systemd 的日志收集、数据库的本地连接。</p><h2 id="vsock" tabindex="-1">VSock <a class="header-anchor" href="#vsock" aria-label="Permalink to “VSock”">​</a></h2><p>VSock（Virtual Socket）是为虚拟化场景设计的一种 Socket 地址族（AF_VSOCK），它允许宿主机和虚拟机之间或虚拟机之间建立高效的通信通道，而不需要经过传统的网络协议栈。VSock 使用 <code>(CID, port)</code> 地址对，其中 CID（Context ID）标识虚拟机或宿主机，port 标识服务端口。</p><p>VSock 的传输机制依赖于虚拟化平台的实现（如 VMware 的 VMCI、KVM 的 virtio-vsock）。数据通过虚拟化平台的共享内存或 hypervisor 传递，避免了网络协议栈和模拟网络设备的开销，提供了比以太网更高的性能和更低的延迟。VSock 常用于虚拟机的管理代理（如 QEMU Guest Agent）、文件共享、快速数据传输等场景。</p><h2 id="系统调用" tabindex="-1">系统调用 <a class="header-anchor" href="#系统调用" aria-label="Permalink to “系统调用”">​</a></h2><h2 id="应用层" tabindex="-1">应用层 <a class="header-anchor" href="#应用层" aria-label="Permalink to “应用层”">​</a></h2><p>由用户态程序实现，此处不展开。</p>`,89)])])}const u=i(t,[["render",n]]);export{k as __pageData,u as default};
