import{_ as i,o as a,c as e,ai as t}from"./chunks/framework.B85_mR9d.js";const c=JSON.parse('{"title":"性能优化","description":"","frontmatter":{},"headers":[],"relativePath":"client/web/browser/optimize.md","filePath":"client/web/browser/optimize.md"}'),n={name:"client/web/browser/optimize.md"};function h(l,s,p,k,d,o){return a(),e("div",null,[...s[0]||(s[0]=[t(`<h1 id="性能优化" tabindex="-1">性能优化 <a class="header-anchor" href="#性能优化" aria-label="Permalink to “性能优化”">​</a></h1><p>Web 性能优化不是一个通用清单，而是一个&quot;量化指标 → 建立基准 → 定位瓶颈 → 局部微操 → 线上监控&quot;的闭环工程。没有度量就没有优化，所有的优化决策都应该基于明确的性能指标和真实的瓶颈定位，而非凭感觉调参。</p><h2 id="core-web-vitals" tabindex="-1">Core Web Vitals <a class="header-anchor" href="#core-web-vitals" aria-label="Permalink to “Core Web Vitals”">​</a></h2><p>Google 提出的 Core Web Vitals 是当前业界衡量 Web 性能的核心指标体系，涵盖了加载体验、交互响应和视觉稳定性三个维度。理解这些指标的底层物理意义，是做性能优化的第一步。</p><h3 id="lcp-最大内容渲染" tabindex="-1">LCP（最大内容渲染） <a class="header-anchor" href="#lcp-最大内容渲染" aria-label="Permalink to “LCP（最大内容渲染）”">​</a></h3><p>LCP（Largest Contentful Paint）衡量的是页面主要内容首次渲染完成的时间，通常是首屏最大的图片、文本块或视频元素完成加载并渲染到屏幕上的时刻。LCP 小于 2.5 秒为优秀，超过 4 秒则需要优化。</p><p>LCP 元素的资源加载是最大的瓶颈。常见的优化手段是提高 LCP 元素的下载优先级。在 HTML 的 <code>&lt;head&gt;</code> 中通过 <code>&lt;link rel=&quot;preload&quot; as=&quot;image&quot; href=&quot;hero.jpg&quot;&gt;</code> 预加载关键图片，配合 <code>fetchpriority=&quot;high&quot;</code> 属性告诉浏览器这个资源优先级高于页面中的其他静态资源。同时确保 LCP 元素的 CSS 不被阻塞——如果首屏大图是通过 CSS 背景图实现的，它的加载优先级低于 <code>&lt;img&gt;</code> 标签，这种情况下应改用 <code>&lt;img&gt;</code> 标签或使用 <code>&lt;link rel=&quot;preload&quot; as=&quot;image&quot;&gt;</code> 提升优先级。</p><p>服务端渲染（SSR）是另一个重要的 LCP 优化手段。传统 SPA 的首屏需要等 JS 下载、解析、执行后才能渲染内容，中间是一段&quot;白屏等待期&quot;。SSR 在服务端直接生成 HTML，浏览器收到 HTML 即可渲染首屏内容，LCP 时间等于 HTML 传输时间而非 JS 下载执行时间。React Server Components（RSC）进一步推进了这个思路，服务端组件不生成 JS，只生成 HTML，减少了客户端 JS 的体积。</p><h3 id="cls-累积布局偏移" tabindex="-1">CLS（累积布局偏移） <a class="header-anchor" href="#cls-累积布局偏移" aria-label="Permalink to “CLS（累积布局偏移）”">​</a></h3><p>CLS（Cumulative Layout Shift）衡量的是页面加载过程中视觉内容的意外位移。常见的场景包括：图片加载完成后撑开布局导致下方内容跳动、异步加载的广告位突然插入页面、Web 字体加载后文本回流等。CLS 小于 0.1 为优秀，超过 0.25 会导致用户操作困难。</p><p>CLS 优化的核心原则是为所有异步加载的内容预留空间。对图片和视频，始终在 HTML 中声明明确的 <code>width</code> 和 <code>height</code> 属性或使用 CSS 的 <code>aspect-ratio</code>，浏览器在资源加载前就能计算出元素的空间占位，避免加载后的布局跳动。对广告位和异步组件，使用 CSS <code>min-height</code> 或骨架屏占位。对动态注入的内容（如通过 AJAX 加载的评论列表），在容器元素上预留足够的空间。</p><p>字体加载也是 CLS 的常见来源。<code>@font-face</code> 加载完成后文本从回退字体切换为设计字体，字符宽度的变化可能导致整行甚至整段文本回流。解决方案是使用 <code>font-display: optional</code>（如果回退字体可接受，直接放弃字体加载）或在字体加载完成前隐藏受影响的文本（<code>font-display: block</code> 配合较短的 block period），避免可见的文本跳动。</p><h3 id="inp-交互延迟" tabindex="-1">INP（交互延迟） <a class="header-anchor" href="#inp-交互延迟" aria-label="Permalink to “INP（交互延迟）”">​</a></h3><p>INP（Interaction to Next Paint）在 2026 年已经全面取代 FID（First Input Delay）成为衡量交互响应能力的核心指标。INP 观察用户在整个页面访问期间的所有交互（点击、键盘、触摸），取其中最差的交互响应时间作为最终得分。INP 小于 200ms 为优秀，超过 500ms 需要优化。</p><p>INP 劣化的根本原因是主线程被长任务（Long Task，执行时间超过 50ms 的任务）阻塞。当用户点击按钮时，浏览器需要等待当前正在执行的长任务完成后才能处理点击事件的回调，这段时间就是交互延迟。</p><p>定位长任务的工具是 Chrome DevTools 的 Performance 面板。录制一段用户交互后，在火焰图（Flame Chart）中寻找标红的长任务条，查看是哪个函数调用链导致了主线程的长时间占用。常见的长任务来源包括大数据量的 DOM 操作、复杂的计算逻辑、同步的 JSON 解析等。</p><p>解决长任务的标准手法是任务切片。将一个耗时数百毫秒的大任务拆分为多个小于 16ms 的小任务（与一帧的时间对齐），通过 <code>requestIdleCallback</code>、<code>MessageChannel</code> 或 <code>setTimeout(fn, 0)</code> 让出主线程控制权，让浏览器有机会处理用户的交互事件和渲染更新，然后在下一个空闲时段继续执行剩余任务。Web Workers 则适合处理计算密集型且与 DOM 无关的任务，将计算过程完全移出主线程。</p><div class="language-javascript"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 任务切片：将大任务拆分为小任务，避免阻塞主线程</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> processInChunks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">items</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">chunkSize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">processor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> items.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> chunkSize) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> chunk</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> items.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">slice</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(i, i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> chunkSize)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    processor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(chunk)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 让出主线程，允许浏览器处理交互事件和渲染</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    await</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">resolve</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> setTimeout</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(resolve, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h2 id="首屏加载优化" tabindex="-1">首屏加载优化 <a class="header-anchor" href="#首屏加载优化" aria-label="Permalink to “首屏加载优化”">​</a></h2><p>首屏加载优化需要按照&quot;网络传输 → 资源体积 → 执行时机&quot;的管线流转来系统性思考，而不是零散地堆砌优化手段。</p><h3 id="网络传输层" tabindex="-1">网络传输层 <a class="header-anchor" href="#网络传输层" aria-label="Permalink to “网络传输层”">​</a></h3><p>HTTP/2 的多路复用消除了浏览器对同一域名的并发连接限制（HTTP/1.1 下通常为 6 个），一个 TCP 连接上可以并行传输多个请求和响应。HTTP/3（QUIC）进一步解决了 TCP 的队头阻塞问题——HTTP/2 在一个 TCP 连接上多路复用，但 TCP 层的丢包重传会导致所有流被阻塞，HTTP/3 基于 UDP 实现的 QUIC 协议让每个流独立恢复，不存在队头阻塞。</p><p>缓存策略的设计需要在请求频率和缓存有效性之间取得平衡。第三方基础库（React、Vue、工具库等）版本变动极少，使用 <code>Cache-Control: max-age=31536000, immutable</code> 强缓存一年，配合内容哈希的文件名——只有文件名变了浏览器才会请求新版本。入口文件 <code>index.html</code> 使用 <code>no-cache</code> 配合 <code>ETag</code> 协商缓存，确保每次都能拿到最新版本的资源引用。这种&quot;长期缓存 + 入口不缓存&quot;的组合是前端部署的标准实践。</p><p>CDN 将静态资源分发到离用户最近的边缘节点，大幅缩短网络传输的物理距离。在 CDN 配置中，需要注意缓存层和源站的一致性——当 CDN 节点缓存了旧版本的资源但用户获取到了新版本的 HTML 时，会出现资源引用不匹配的 404 错误。工程上通常在资源文件名中嵌入内容哈希来规避这个问题。</p><h3 id="资源体积层" tabindex="-1">资源体积层 <a class="header-anchor" href="#资源体积层" aria-label="Permalink to “资源体积层”">​</a></h3><p>代码分割（Code Splitting）是控制首屏加载体积的核心手段。通过路由级别的动态导入（<code>import()</code>），首屏只下载当前路由必需的代码，其余路由的代码在用户导航时按需加载。构建工具（Vite 的 <code>manualChunks</code>、Webpack 的 <code>splitChunks</code>）将框架代码、工具库、业务代码分离为独立的 chunk，变化频率不同的代码分开缓存——框架代码几乎不变，业务代码频繁变动，混在一起会导致业务改动让框架缓存全部失效。</p><p>压缩方面，服务端配置 Brotli 或 Gzip 压缩传输文本资源（JS、CSS、HTML、SVG），通常能减少 60%-80% 的传输体积。Brotli 的压缩率比 Gzip 高 15%-25%，但压缩耗时更长，适合静态资源在构建时预压缩后直接部署，而非实时压缩。</p><p>图片优化在首屏体积中占比极大。现代方案包括：根据设备 DPR 使用 <code>&lt;picture&gt;</code> 和 <code>srcset</code> 提供不同分辨率的图片，使用 WebP/AVIF 格式替代 JPEG/PNG（AVIF 在同等画质下比 JPEG 小 50%），以及按视口尺寸裁剪图片避免下载远超屏幕尺寸的大图。</p><h3 id="关键渲染路径" tabindex="-1">关键渲染路径 <a class="header-anchor" href="#关键渲染路径" aria-label="Permalink to “关键渲染路径”">​</a></h3><p>浏览器从收到 HTML 到渲染首屏，经历了一系列有序的步骤：解析 HTML 构建 DOM 树 → 解析 CSS 构建 CSSOM 树 → 合并为渲染树 → 计算布局 → 绘制。这个流程中，阻塞渲染的关键资源（通常是 CSS）越多，首屏渲染越晚。</p><p>CSS 放在 <code>&lt;head&gt;</code> 中尽早加载，JS 使用 <code>defer</code> 或 <code>async</code> 属性避免阻塞 HTML 解析。<code>defer</code> 告诉浏览器在 HTML 解析完成后、DOMContentLoaded 之前按顺序执行脚本；<code>async</code> 让脚本在下载完成后立即执行，不保证顺序也不等待 HTML 解析完成。对于不参与首屏渲染的第三方脚本（统计、客服、广告），使用 <code>async</code> 或动态注入 <code>&lt;script&gt;</code> 标签，将它们从首屏关键路径中完全剥离。</p><h2 id="虚拟滚动" tabindex="-1">虚拟滚动 <a class="header-anchor" href="#虚拟滚动" aria-label="Permalink to “虚拟滚动”">​</a></h2><p>长列表渲染是前端性能的经典问题。当列表包含上千甚至上万条数据时，直接将所有节点渲染到 DOM 中会导致严重的性能问题——DOM 节点数量过多，每次滚动都触发大量节点的布局计算和绘制，帧率急剧下降。</p><p>虚拟滚动的核心原理是只渲染当前可视区域内的少量 DOM 节点。通过监听容器的滚动事件，计算 <code>scrollTop</code> 和每项高度，动态切片数据数组（<code>slice(startIndex, endIndex)</code>），只将可视区间内的条目渲染为 DOM 节点。为了保持滚动条的正确长度和滚动时的视觉连续性，通常在容器内添加一个高度等于总列表高度的空间撑开元素（spacer），然后通过 <code>transform: translateY()</code> 将可视节点定位到正确的物理位置。</p><div class="language-javascript"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> getVisibleRange</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">scrollTop</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">itemHeight</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">viewportHeight</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">totalItems</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> startIndex</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Math.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">floor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(scrollTop </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> itemHeight)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> endIndex</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Math.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">min</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    startIndex </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Math.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ceil</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(viewportHeight </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> itemHeight) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    totalItems</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  )</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { startIndex, endIndex }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>当列表项高度不固定时（如朋友圈、带评论的卡片），虚拟滚动的实现复杂度显著增加。核心策略是&quot;预估高度 + 动态修正&quot;：初始化时为每条数据设定一个预估高度，计算出大致的总高度撑开滚动条；当节点真正渲染到 DOM 后，通过 <code>ResizeObserver</code> 或 <code>getBoundingClientRect()</code> 捕获其真实高度，动态更新一个位置索引缓存表（Offset Map），记录每条数据到顶部的累计偏移量。后续滚动时通过二分查找这个索引表快速定位可视区间。为了防止高频滚动导致 <code>ResizeObserver</code> 密集触发引发掉帧，需要配合 <code>requestAnimationFrame</code> 进行节流。</p><h2 id="图片懒加载" tabindex="-1">图片懒加载 <a class="header-anchor" href="#图片懒加载" aria-label="Permalink to “图片懒加载”">​</a></h2><p>图片懒加载的目标是推迟视口外图片的加载时机，减少首屏的网络请求和带宽消耗。</p><p>早期的实现方案是在 <code>scroll</code> 事件中通过 <code>getBoundingClientRect()</code> 判断图片是否进入视口。这个方案有明显的性能问题：<code>scroll</code> 事件触发极其频繁，且 <code>getBoundingClientRect()</code> 的调用会强制浏览器执行同步布局重算（Forced Synchronous Layout），在滚动期间反复触发会导致严重的卡顿。</p><p>现代方案是使用 <code>IntersectionObserver</code>。它基于浏览器底层的异步机制，只在元素与视口发生交叉时触发回调，不需要在主线程上频繁计算位置。被观察的图片在进入视口时才设置 <code>src</code> 属性发起加载，离开视口后可以选择清除 <code>src</code> 释放内存。</p><p>更简洁的方案是使用浏览器原生的 <code>loading=&quot;lazy&quot;</code> 属性。在 <code>&lt;img&gt;</code> 标签上声明后，浏览器自动推迟视口外图片的加载，无需任何 JavaScript。现代浏览器（Chrome、Firefox、Edge、Safari）均已支持该属性。在实际项目中，首屏可见的关键图片不使用懒加载（确保 LCP 不受影响），首屏以下的图片统一使用原生懒加载，这是成本最低、效果最好的图片优化策略。</p><h2 id="内存泄漏排查" tabindex="-1">内存泄漏排查 <a class="header-anchor" href="#内存泄漏排查" aria-label="Permalink to “内存泄漏排查”">​</a></h2><p>内存泄漏是区分&quot;真有实战经验&quot;和&quot;纯背八股&quot;的分水岭。典型的泄漏场景包括三种：组件销毁时未清除定时器（<code>setInterval</code>、<code>setTimeout</code>）或事件监听器（<code>addEventListener</code>），导致闭包持有组件上下文无法被 GC 回收；在 JavaScript 中保存了 DOM 节点的引用后通过 <code>removeChild</code> 从页面删除了该节点（Detached DOM），DOM 节点虽然不可见但仍占据内存；全局变量或缓存不断增长但从不清理，随着应用运行时间增长内存持续膨胀。</p><p>排查内存泄漏的标准流程基于 Chrome DevTools 的 Memory 面板。第一步，在页面初始化时拍下堆快照（Heap Snapshot）作为基准。第二步，反复执行疑似泄漏的操作（如打开关闭弹窗十次），拍下第二张快照。第三步，点击垃圾桶图标强制触发垃圾回收，拍下第三张快照。第四步，使用 Memory 面板的 Comparison（对比）功能，将第三张快照与第一张进行比对，筛选出增长异常的对象。重点查看 <code>Detached HTMLDivElement</code>（游离 DOM 节点）和特定组件的构造函数实例。通过 Retainers（持有者引用链）面板向上追溯，可以精确定位是哪个全局变量、闭包或未清理的事件监听器锁死了这块内存，然后在代码中对应的清理逻辑处进行修复。</p><p>在日常开发中，养成在组件销毁生命周期（如 Vue 的 <code>onUnmounted</code>、React 的 <code>useEffect</code> 返回清理函数）中主动清除副作用（移除事件监听、清除定时器、断开 WebSocket 连接）的习惯，可以避免绝大多数内存泄漏问题。使用弱引用（<code>WeakMap</code>、<code>WeakSet</code>）替代强引用来缓存对象，也能让 GC 在需要时自动回收这些对象。</p>`,45)])])}const E=i(n,[["render",h]]);export{c as __pageData,E as default};
