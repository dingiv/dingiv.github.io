import{_ as i,o as a,c as e,ai as n}from"./chunks/framework.B85_mR9d.js";const E=JSON.parse('{"title":"时钟中断","description":"","frontmatter":{},"headers":[],"relativePath":"kernel/linux/irq/clock.md","filePath":"kernel/linux/irq/clock.md"}'),t={name:"kernel/linux/irq/clock.md"};function l(h,s,k,p,r,d){return a(),e("div",null,[...s[0]||(s[0]=[n(`<h1 id="时钟中断" tabindex="-1">时钟中断 <a class="header-anchor" href="#时钟中断" aria-label="Permalink to “时钟中断”">​</a></h1><p>时钟中断是操作系统的心跳信号，由定时器硬件周期性触发，内核在每个时钟中断中执行时间维护和任务调度等基础工作。时钟中断的频率直接决定了系统的计时精度和调度粒度。</p><h2 id="硬件定时器" tabindex="-1">硬件定时器 <a class="header-anchor" href="#硬件定时器" aria-label="Permalink to “硬件定时器”">​</a></h2><p>内核依赖硬件定时器产生周期性的时钟中断。不同平台上使用的定时器硬件不同。</p><p>在 x86 平台上，历史上有三种硬件定时器：<strong>PIT（Programmable Interval Timer，8254）</strong> 是最早的 PC 定时器，精度约 1μs，通过 I/O 端口 0x40-0x43 访问，目前已基本淘汰；<strong>HPET</strong> 是高精度事件定时器，精度可达 100ns 甚至 10ns，通过 MMIO 访问，支持多个独立的比较器通道；<strong>LAPIC Timer</strong> 是集成在每个 CPU 核心的 Local APIC 内部的定时器，每个核心独立运行，精度高且不需要经过总线访问，是现代 x86 系统首选的 per-CPU 时钟源。</p><p>在 ARM 平台上，GIC 的分发器可以管理全局定时器（Global Timer），此外每个 CPU 核心还有私有定时器（Private Timer）和看门狗定时器。Cortex-A 系列的通用定时器（Generic Timer）通过系统寄存器访问（<code>CNTV_CTL_EL0</code>、<code>CNTV_TVAL_EL0</code>），提供 per-CPU 的虚拟定时器和物理定时器，内核使用虚拟定时器作为 per-CPU 的 tick 源。</p><h2 id="jiffies-与-tick" tabindex="-1">jiffies 与 tick <a class="header-anchor" href="#jiffies-与-tick" aria-label="Permalink to “jiffies 与 tick”">​</a></h2><p>内核维护一个全局变量 <code>jiffies</code>，每个时钟中断（tick）加一。<code>jiffies</code> 是内核中最基础的时间计量单位，内核中大量超时判断、时间计算都基于 jiffies。<code>CONFIG_HZ</code> 配置项定义了每秒的 tick 次数，常见值有 100（10ms 精度）、250（4ms 精度）和 1000（1ms 精度）。服务器平台通常使用 1000Hz 以获得更好的调度响应性。</p><p>时钟中断的处理函数 <code>tick_handle_periodic</code> 主要完成以下工作：更新 <code>jiffies</code>；更新系统的墙上时间（wall time，基于 <code>xtime</code> 和 <code>wall_to_monotonic</code>）；检查是否有到期的内核定时器（<code>timer wheel</code>）；调用调度器（<code>scheduler_tick</code>）更新进程的时间片和统计信息；处理 RCU 相关的宽限期检查。</p><p>传统模式下，无论 CPU 是否有空闲任务，时钟中断都以固定频率触发。这对于需要精细计时的场景是必要的，但在 CPU 空闲时，周期性的时钟中断会频繁唤醒 CPU，阻止其进入深度休眠状态，浪费功耗。</p><h2 id="no-hz-与-tickless" tabindex="-1">NO_HZ 与 tickless <a class="header-anchor" href="#no-hz-与-tickless" aria-label="Permalink to “NO_HZ 与 tickless”">​</a></h2><p><code>CONFIG_NO_HZ_IDLE</code>（也称 tickless idle）模式下，当 CPU 进入空闲状态（没有可运行的进程）时，内核停止该 CPU 上的周期性时钟中断，改为设置一个一次性定时器，在下一个最近的到期事件（最近的定时器到期或调度需求）时唤醒 CPU。这样 CPU 可以在空闲期间持续停留在低功耗状态，而不是被无意义的时钟中断反复唤醒。</p><p><code>CONFIG_NO_HZ_FULL</code>（full tickless）更进一步，即使 CPU 在执行任务时也取消周期性 tick，改为按需触发。这适用于运行一个或少量 CPU 密集型任务的场景（如高性能网络转发、HPC 计算），消除了时钟中断带来的上下文切换和缓存抖动开销。但 full tickless 模式下，基于 jiffies 的时间统计会不准确，需要依赖高精度定时器补偿。</p><h2 id="高精度定时器-hrtimer" tabindex="-1">高精度定时器（hrtimer） <a class="header-anchor" href="#高精度定时器-hrtimer" aria-label="Permalink to “高精度定时器（hrtimer）”">​</a></h2><p>内核的定时器框架分为两层：基于 jiffies 的<strong>低精度定时器</strong>（<code>timer_list</code>，精度为 1/HZ 秒）和基于硬件时钟源的<strong>高精度定时器</strong>（<code>hrtimer</code>，精度可达纳秒级）。现代内核中 hrtimer 是主要的定时器实现，低精度定时器在底层也通过 hrtimer 模拟。</p><p>hrtimer 的核心是一个按到期时间排序的红黑树（per-CPU），每个 CPU 维护独立的红黑树。添加定时器时将其插入对应 CPU 的红黑树，硬件定时器被编程为在最近的到期时间触发中断。中断到达时，内核从红黑树中取出所有已到期的定时器并执行其回调函数，然后将硬件定时器编程为下一个到期时间。</p><p>hrtimer 的时钟源是 <code>clocksource</code>（如 TSC、ARM Generic Timer），精度取决于硬件时钟源的能力。在 x86 上，TSC 是最优的时钟源；在 ARM 上，Generic Timer 是首选。<code>/sys/devices/system/clocksource/clocksource0/</code> 可以查看和切换时钟源。</p><div class="language-c"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// hrtimer 的典型使用方式</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;linux/hrtimer.h&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">enum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> hrtimer_restart </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">my_timer_callback</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> hrtimer </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">timer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> my_device </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dev </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> container_of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(timer, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> my_device, timer);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 定时器到期时的处理逻辑</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    handle_timeout</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(dev);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 返回 HRTIMER_NORESTART 表示一次性定时器</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 返回 HRTIMER_RESTART 表示周期性定时器（需要重新设置到期时间）</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> HRTIMER_NORESTART;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 初始化</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">hrtimer_init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dev</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">timer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, CLOCK_MONOTONIC, HRTIMER_MODE_REL);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dev</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">timer.function </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> my_timer_callback;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 启动定时器，500ms 后到期</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">hrtimer_start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dev</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">timer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ms_to_ktime</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">500</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), HRTIMER_MODE_REL);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 取消定时器</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">hrtimer_cancel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dev</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">timer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>hrtimer 的回调函数运行在硬中断上下文（hrtimer 中断），因此不能休眠。如果定时器回调需要执行耗时操作，应结合 workqueue 使用。</p><h2 id="与调度器的关系" tabindex="-1">与调度器的关系 <a class="header-anchor" href="#与调度器的关系" aria-label="Permalink to “与调度器的关系”">​</a></h2><p>时钟中断是调度器运行的驱动力量。每次 tick 中断都会调用 <code>scheduler_tick</code>，执行以下工作：更新当前进程的运行时间统计（<code>vruntime</code>）；检查当前进程的时间片是否用尽，如果用尽则设置 <code>TIF_NEED_RESCHED</code> 标志，在时钟中断返回用户态时触发调度。</p><p>在 NO_HZ 模式下，时钟中断不再周期性触发，调度器的时间片到期检测依赖于高精度定时器。内核为每个进程设置一个调度到期定时器（sched timer），当进程的时间片用完时该定时器触发，产生一次高精度定时器中断，在中断中完成调度切换。这意味着即使没有周期性 tick，调度器仍然能正确地剥夺超时进程的 CPU。</p><p>从驱动开发的角度来看，时钟中断通常是不可见的——驱动使用定时器 API（hrtimer 或 timer_list）来设置超时，内核负责在合适的硬件定时器上编程。只有在内核调试或性能分析时，才需要关注时钟中断的频率、NO_HZ 模式和时钟源选择。</p>`,23)])])}const o=i(t,[["render",l]]);export{E as __pageData,o as default};
