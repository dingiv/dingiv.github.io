import{H as e,V as t,et as n,j as r}from"./chunks/framework.C_EfTa_6.js";var i=JSON.parse(`{"title":"命名空间","description":"","frontmatter":{"title":"命名空间","order":30},"headers":[],"relativePath":"kernel/linux/process/namespace.md","filePath":"kernel/linux/process/namespace.md"}`),a={name:`kernel/linux/process/namespace.md`};function o(r,i,a,o,s,c){return n(),t(`div`,null,[...i[0]||=[e(`<h1 id="命名空间" tabindex="-1">命名空间 <a class="header-anchor" href="#命名空间" aria-label="Permalink to “命名空间”">​</a></h1><p>Namespace 是 Linux 容器技术的基石。它的核心思想朴素而深刻：内核资源是全局的，但可以让每个进程看到一套<strong>裁剪过的视图</strong>——进程以为自己是 PID 1，以为自己是 root，以为拥有整张网络接口列表，而实际上这些视图都是内核为它专门构造的假象。</p><p>从内核实现的角度看，Namespace 做的事情是把六类全局资源&quot;按命名空间切分&quot;：进程号空间、网络栈、挂载点、主机标识、用户权限、IPC 对象。每个进程通过 <code>task_struct</code> 中的 <code>nsproxy</code> 指针关联到一组命名空间——这个结构体里存放的七个指针（对应七种命名空间）共同定义了进程眼中的世界。</p><h2 id="六大类资源隔离" tabindex="-1">六大类资源隔离 <a class="header-anchor" href="#六大类资源隔离" aria-label="Permalink to “六大类资源隔离”">​</a></h2><h3 id="进程号空间-pid-namespace" tabindex="-1">进程号空间：PID Namespace <a class="header-anchor" href="#进程号空间-pid-namespace" aria-label="Permalink to “进程号空间：PID Namespace”">​</a></h3><p>PID Namespace 隔离的是进程编号的可视范围。没有 PID Namespace 之前，全局只有一个 PID 空间，所有进程按创建顺序分配唯一编号。引入 PID Namespace 后，每个命名空间内的进程从 1 开始独立编号——容器内那个&quot;PID 1&quot;在宿主机上可能实际编号 18472。</p><p>内核实现的核心是一个层级化的 <code>pid_namespace</code> 结构。每个命名空间记录自己内部的编号分配，同时通过 <code>parent</code> 指针链接到上层命名空间——一个进程在每个祖先命名空间中都有对应的编号。<code>getpid()</code> 系统调用返回当前命名空间内的编号，而宿主机的 <code>ps</code> 看到的是初始命名空间内的编号。</p><p>有两个实现细节值得注意。一是 PID 1 的特殊性：每个 PID Namespace 内的 init 进程负责收养孤儿进程——init 退出时，内核向命名空间内所有进程发送 SIGKILL（<code>pidns-&gt;reboot</code> 机制），整个命名空间被强制清理。这正是容器内&quot;主进程退出则容器退出&quot;的内核根源。二是信号发送的权限限制：父命名空间的进程可以向子命名空间内的进程发信号（通过 <code>kill</code> 系统调用带 PID 转换），反之则不允许——这是跨命名空间信号隔离的规则。</p><h3 id="网络栈-network-namespace" tabindex="-1">网络栈：Network Namespace <a class="header-anchor" href="#网络栈-network-namespace" aria-label="Permalink to “网络栈：Network Namespace”">​</a></h3><p>Network Namespace 隔离的是整套网络栈——不只是网络接口，还包括路由表、iptables 规则、ARP 表、socket 端口空间。每个 Network Namespace 拥有独立的 <code>struct net</code> 实例，内核网络子系统的所有全局变量在这个结构体中都有对应副本。</p><p>工程上最有价值的细节是 <strong>lo 接口在命名空间间不共享</strong>。新创建的 Network Namespace 默认只有 down 状态的 loopback——容器网络的第一步永远是&quot;把 lo 拉起来&quot;。宿主机与容器通信通过 veth pair：veth 是一对逻辑相连的网卡，一端留在宿主机命名空间（通常挂到 bridge 上），另一端移入容器命名空间，两者之间的流量就是容器进出网络的通道。</p><p>端口空间的隔离是 Network Namespace 最直观的收益——两个容器可以各自监听 80 端口互不冲突，因为它们的 <code>bind</code> 查找发生在各自的 <code>struct net</code> 的端口哈希表中。</p><h3 id="挂载视图-mount-namespace" tabindex="-1">挂载视图：Mount Namespace <a class="header-anchor" href="#挂载视图-mount-namespace" aria-label="Permalink to “挂载视图：Mount Namespace”">​</a></h3><p>Mount Namespace 隔离的是挂载点列表。每个命名空间维护自己的挂载树（内核 <code>struct mount</code> 的独立实例），<code>mount</code>/<code>umount</code> 操作只影响当前命名空间。</p><p>Mount Namespace 是容器镜像分层机制的根基。容器启动时，运行时在当前命名空间中挂载容器的 rootfs，配合 OverlayFS 把镜像层叠加为一个合并视图——这个&quot;根目录&quot;只存在于容器的 Mount Namespace 中，宿主机看到的仍是自己的根目录。<code>pivot_root</code> 系统调用在这里发挥作用：它把容器的 rootfs 挂载变为命名空间的根，同时把宿主机的旧根目录移走——彻底切断容器对宿主机目录树的可见路径。</p><p>Mount Namespace 也是传播机制（propagation）的试验场：<code>mount --make-shared/private/slave</code> 控制挂载事件在命名空间间的传播方向。Docker 卷挂载的某些行为（宿主机挂载在容器内可见，容器内挂载在宿主机不可见）就依赖这个机制。</p><h3 id="主机标识-uts-namespace" tabindex="-1">主机标识：UTS Namespace <a class="header-anchor" href="#主机标识-uts-namespace" aria-label="Permalink to “主机标识：UTS Namespace”">​</a></h3><p>UTS Namespace 隔离的是主机名（hostname）和 NIS 域名——通过 <code>sethostname</code> 和 <code>setdomainname</code> 系统调用设置的标识。它是六类命名空间中实现最简单的（<code>struct uts_namespace</code> 只有两个字符串字段），但意义不小：没有它，容器内修改主机名会影响宿主机，而且 <code>hostname</code> 命令的隔离感是容器&quot;独立系统&quot;体验的一部分。</p><h3 id="用户与权限-user-namespace" tabindex="-1">用户与权限：User Namespace <a class="header-anchor" href="#用户与权限-user-namespace" aria-label="Permalink to “用户与权限：User Namespace”">​</a></h3><p>User Namespace 是六类命名空间中最后加入、也是安全影响最大的。它隔离的是 UID/GID 的映射关系：命名空间内的 UID 0（root）可以映射为宿主机上的非特权 UID（如 1000）。</p><p>实现上，每个进程的凭证（<code>struct cred</code>）在 User Namespace 的上下文中解释。容器内进程以 UID 0 运行——拥有该命名空间内全部 capabilities——但宿主机视角它只是普通用户 1000。<strong>Capabilities 的作用域也被 User Namespace 限制</strong>：进程持有的 capability 只在拥有它的 User Namespace 及其子命名空间内有效——容器内 root 的 <code>CAP_SYS_ADMIN</code> 不能操作宿主机的任何资源。</p><p>这是 rootless 容器（Podman rootless、Docker rootless mode）的技术基础：普通用户通过 <code>unshare -U</code> 创建 User Namespace 让自己在其中成为 root，再在这个命名空间内创建其他命名空间——全程不需要宿主机 root 权限。</p><h3 id="进程间通信-ipc-namespace" tabindex="-1">进程间通信：IPC Namespace <a class="header-anchor" href="#进程间通信-ipc-namespace" aria-label="Permalink to “进程间通信：IPC Namespace”">​</a></h3><p>IPC Namespace 隔离的是 System V IPC 对象（共享内存段 <code>shmget</code>、信号量 <code>semget</code>、消息队列 <code>msgget</code>）和 POSIX 消息队列。这些对象的命名空间是全局 ID 分配的——<code>shmget(key)</code> 中的 key 在各命名空间独立解析，一个命名空间内创建的共享内存段对另一个命名空间完全不可见。</p><p>IPC 隔离对容器安全有实际意义：不隔离时，宿主机和容器之间可能通过已知 key 的共享内存段意外共享数据。IPC Namespace 关闭了这个通道。</p><h3 id="两种较新的命名空间" tabindex="-1">两种较新的命名空间 <a class="header-anchor" href="#两种较新的命名空间" aria-label="Permalink to “两种较新的命名空间”">​</a></h3><p>Cgroup Namespace 隔离的是 <code>/proc/self/cgroup</code> 和 <code>/sys/fs/cgroup</code> 的视图——容器内的进程看到自己的 cgroup 路径是 <code>/</code>，而非宿主机上的完整路径。这是 2016 年（内核 4.6）加入的，动机是防止容器内的进程通过 cgroup 路径推断宿主机布局。</p><p>Time Namespace（内核 5.6）隔离的是 CLOCK_MONOTONIC 和 CLOCK_BOOTTIME——允许容器拥有独立的单调时钟起点。这对容器快照/恢复（CRIU）有意义：恢复的容器不应看到快照期间的时钟跳变。</p><h2 id="命名空间的生命周期" tabindex="-1">命名空间的生命周期 <a class="header-anchor" href="#命名空间的生命周期" aria-label="Permalink to “命名空间的生命周期”">​</a></h2><h3 id="创建-clone-与-unshare" tabindex="-1">创建：clone 与 unshare <a class="header-anchor" href="#创建-clone-与-unshare" aria-label="Permalink to “创建：clone 与 unshare”">​</a></h3><p>创建命名空间的两个系统调用分工明确。<code>clone</code> 在创建新进程的同时创建命名空间——<code>clone(CLONE_NEWPID | CLONE_NEWNET, ...)</code> 一次调用产生一个处于新命名空间中的新进程，容器运行时的 init 进程就是这样创建的。<code>unshare</code> 不创建进程，把调用者自身移入新命名空间——<code>unshare -n</code> 让当前 shell 获得独立网络栈。</p><p>一个容易被误解的细节：<strong>PID Namespace 通过 clone 创建时，新进程是命名空间内的 PID 1，但通过 unshare 创建时调用者不是 PID 1</strong>。unshare 后第一个 fork 的子进程才成为 PID 1。这意味着 unshare 方式创建 PID Namespace 时必须立即 fork——否则命名空间内没有 init 进程，后续进程无法正常创建。</p><h3 id="关联-setns" tabindex="-1">关联：setns <a class="header-anchor" href="#关联-setns" aria-label="Permalink to “关联：setns”">​</a></h3><p><code>setns(fd, nstype)</code> 让调用进程加入一个已存在的命名空间，参数是 <code>/proc/&lt;pid&gt;/ns/&lt;type&gt;</code> 的文件描述符。这是 <code>docker exec</code> 进入容器、<code>nsenter</code> 工具的实现基础——这些工具打开目标进程的命名空间文件，然后 setns 进去。</p><p><code>/proc/&lt;pid&gt;/ns/</code> 目录下的文件是命名空间对象的&quot;引用&quot;——内核用这些文件跟踪命名空间对象的引用计数。一个命名空间在所有进程退出、所有引用文件被关闭后才被销毁。这也是为什么一个进程可以&quot;握住&quot;一个已经没有进程的命名空间，随时再进去。</p><h3 id="销毁" tabindex="-1">销毁 <a class="header-anchor" href="#销毁" aria-label="Permalink to “销毁”">​</a></h3><p>命名空间对象采用引用计数管理。进程退出时递减其所处命名空间的计数；<code>/proc/&lt;pid&gt;/ns/</code> 的打开文件也各持一个引用。计数归零时内核释放命名空间对象——Network Namespace 的释放会触发网络栈清理（释放接口、路由表、socket），Mount Namespace 的释放触发挂载树清理。这个&quot;最后一个引用消失才销毁&quot;的语义，是 CRIU 等容器迁移工具能够工作的重要前提：它们在迁移过程中持有的命名空间文件描述符，保证了命名空间在进程暂停期间不消失。</p><h2 id="从内核到容器" tabindex="-1">从内核到容器 <a class="header-anchor" href="#从内核到容器" aria-label="Permalink to “从内核到容器”">​</a></h2><p>Namespace 本身只是视图隔离——六类资源的&quot;可见性切分&quot;。完整的容器还需要另外三块拼图：Cgroup 提供资源计量与限制（&quot;能用多少&quot;）、联合文件系统提供镜像分层（&quot;根目录从哪来&quot;）、Capabilities/Seccomp/SELinux 提供真正的安全约束（&quot;能做什么&quot;）。Namespace 解决的是容器&quot;看起来是独立系统&quot;的问题，而容器安全是另一套机制的职责——把视图隔离误当作安全隔离，是理解容器最深的坑。</p><p>Namespace 的价值也不局限于容器：<code>unshare -m</code> 在普通用户态就提供沙箱化挂载视图，测试挂载脚本时不用污染系统；Network Namespace 被广泛用于网络功能虚拟化（每个虚拟网络功能一个命名空间）和进程级 VPN 分流。容器只是这个内核特性的最大用户，不是它的全部。</p>`,40)]])}var s=r(a,[[`render`,o]]);export{i as __pageData,s as default};