import{_ as e,o as r,c as h,ah as i}from"./chunks/framework.C2p4DT9L.js";const b=JSON.parse('{"title":"TCP","description":"","frontmatter":{"title":"TCP","order":2},"headers":[],"relativePath":"server/network/proto/tcp.md","filePath":"server/network/proto/tcp.md"}'),l={name:"server/network/proto/tcp.md"};function t(n,a,p,d,c,s){return r(),h("div",null,[...a[0]||(a[0]=[i('<h1 id="tcp-协议" tabindex="-1">TCP 协议 <a class="header-anchor" href="#tcp-协议" aria-label="Permalink to “TCP 协议”">​</a></h1><p>TCP（Transmission Control Protocol，传输控制协议）是面向连接的、可靠的、基于字节流的传输层协议。TCP 解决了数据传输的可靠性、顺序性、流量控制、拥塞控制等问题。</p><h2 id="tcp-头部格式" tabindex="-1">TCP 头部格式 <a class="header-anchor" href="#tcp-头部格式" aria-label="Permalink to “TCP 头部格式”">​</a></h2><h3 id="源端口和目的端口" tabindex="-1">源端口和目的端口 <a class="header-anchor" href="#源端口和目的端口" aria-label="Permalink to “源端口和目的端口”">​</a></h3><p>16 位，标识发送方和接收方的进程。端口号范围：0-65535，0-1023 为知名端口（如 HTTP 80、HTTPS 443）。</p><h3 id="序列号" tabindex="-1">序列号 <a class="header-anchor" href="#序列号" aria-label="Permalink to “序列号”">​</a></h3><p>32 位，标识 TCP 段中第一个字节的序号。序列号是循环使用的，达到 2^32-1 后从 0 开始。</p><h3 id="确认号" tabindex="-1">确认号 <a class="header-anchor" href="#确认号" aria-label="Permalink to “确认号”">​</a></h3><p>32 位，期望收到的下一个字节的序号。确认号是累积确认，表示之前的字节都已正确接收。</p><h3 id="数据偏移" tabindex="-1">数据偏移 <a class="header-anchor" href="#数据偏移" aria-label="Permalink to “数据偏移”">​</a></h3><p>4 位，TCP 头部的长度，以 4 字节为单位。最小值 5（20 字节），最大值 15（60 字节）。</p><h3 id="标志位" tabindex="-1">标志位 <a class="header-anchor" href="#标志位" aria-label="Permalink to “标志位”">​</a></h3><p>URG（紧急指针有效）、ACK（确认号有效）、PSH（推送）、RST（重置连接）、SYN（同步序列号）、FIN（结束连接）。</p><h3 id="窗口大小" tabindex="-1">窗口大小 <a class="header-anchor" href="#窗口大小" aria-label="Permalink to “窗口大小”">​</a></h3><p>16 位，接收窗口的大小，用于流量控制。窗口大小最大 65535 字节，通过窗口扩大选项可以扩大到 1GB。</p><h3 id="校验和" tabindex="-1">校验和 <a class="header-anchor" href="#校验和" aria-label="Permalink to “校验和”">​</a></h3><p>16 位，检验 TCP 头部和数据的完整性。发送方计算校验和，接收方验证校验和。</p><h3 id="紧急指针" tabindex="-1">紧急指针 <a class="header-anchor" href="#紧急指针" aria-label="Permalink to “紧急指针”">​</a></h3><p>16 位，指向紧急数据的最后一个字节。紧急数据需要优先处理。</p><h3 id="选项" tabindex="-1">选项 <a class="header-anchor" href="#选项" aria-label="Permalink to “选项”">​</a></h3><p>选项字段可变长，常见选项：MSS（最大段大小）、窗口扩大、时间戳、SACK（选择性确认）。</p><h2 id="tcp-连接建立" tabindex="-1">TCP 连接建立 <a class="header-anchor" href="#tcp-连接建立" aria-label="Permalink to “TCP 连接建立”">​</a></h2><h3 id="三次握手" tabindex="-1">三次握手 <a class="header-anchor" href="#三次握手" aria-label="Permalink to “三次握手”">​</a></h3><p>第一次握手：客户端发送 SYN，seq = x。客户端进入 SYN_SENT 状态。</p><p>第二次握手：服务端回复 SYN+ACK，seq = y，ack = x + 1。服务端进入 SYN_RCVD 状态。</p><p>第三次握手：客户端回复 ACK，seq = x + 1，ack = y + 1。双方进入 ESTABLISHED 状态。</p><h3 id="为什么需要三次握手" tabindex="-1">为什么需要三次握手 <a class="header-anchor" href="#为什么需要三次握手" aria-label="Permalink to “为什么需要三次握手”">​</a></h3><p>防止失效的连接请求报文段突然又传送到服务端，产生错误。</p><p>客户端的发送连接请求报文段丢失，客户端重传连接请求报文段，连接建立后数据传输完毕，连接释放。第一个丢失的连接请求报文段延迟到达，服务端误认为新的连接请求，发送确认报文段，进入 ESTABLISHED 状态，等待客户端数据，浪费资源。</p><p>三次握手可以确认双方的接收和发送能力都正常，同步双方的初始序列号。</p><h3 id="syn-攻击" tabindex="-1">SYN 攻击 <a class="header-anchor" href="#syn-攻击" aria-label="Permalink to “SYN 攻击”">​</a></h3><p>SYN 攻击是攻击者发送大量 SYN 报文段，不完成三次握手，占用服务端的半连接队列，导致服务端无法处理正常连接。</p><p>防御措施：SYN Cookies（不保存半连接状态，根据 SYN 计算 Cookie）、缩短超时时间、增加半连接队列大小、开启 tcp_syncookies。</p><h2 id="tcp-连接终止" tabindex="-1">TCP 连接终止 <a class="header-anchor" href="#tcp-连接终止" aria-label="Permalink to “TCP 连接终止”">​</a></h2><h3 id="四次挥手" tabindex="-1">四次挥手 <a class="header-anchor" href="#四次挥手" aria-label="Permalink to “四次挥手”">​</a></h3><p>第一次挥手：主动方发送 FIN，seq = u。主动方进入 FIN_WAIT_1 状态。</p><p>第二次挥手：被动方回复 ACK，ack = u + 1。被动方进入 CLOSE_WAIT 状态。主动方收到 ACK 后进入 FIN_WAIT_2 状态。</p><p>第三次挥手：被动方发送 FIN，seq = w，ack = u + 1。被动方进入 LAST_ACK 状态。</p><p>第四次挥手：主动方回复 ACK，seq = u + 1，ack = w + 1。主动方进入 TIME_WAIT 状态，等待 2MSL 后进入 CLOSED 状态。被动方收到 ACK 后进入 CLOSED 状态。</p><h3 id="为什么需要四次挥手" tabindex="-1">为什么需要四次挥手 <a class="header-anchor" href="#为什么需要四次挥手" aria-label="Permalink to “为什么需要四次挥手”">​</a></h3><p>TCP 是全双工协议，双方都可以发送数据。主动方关闭发送通道，但可能还在接收数据。被动方可能还有数据要发送，需要确认数据发送完毕后再关闭发送通道。</p><h3 id="time-wait-状态" tabindex="-1">TIME_WAIT 状态 <a class="header-anchor" href="#time-wait-状态" aria-label="Permalink to “TIME_WAIT 状态”">​</a></h3><p>TIME_WAIT 状态持续 2MSL，约 60 秒。作用：确保最后的 ACK 能够到达被动方，如果 ACK 丢失，被动方会重传 FIN，主动方可以重发 ACK。确保旧连接的报文段在网络中消失，不影响新连接。</p><p>TIME_WAIT 状态的问题：占用端口资源，高并发短连接场景下端口可能耗尽。解决方案：开启 tcp_tw_reuse、tcp_tw_recycle（Linux 4.12 已移除）、增加端口范围。</p><h2 id="tcp-可靠传输" tabindex="-1">TCP 可靠传输 <a class="header-anchor" href="#tcp-可靠传输" aria-label="Permalink to “TCP 可靠传输”">​</a></h2><h3 id="序列号-1" tabindex="-1">序列号 <a class="header-anchor" href="#序列号-1" aria-label="Permalink to “序列号”">​</a></h3><p>序列号标识 TCP 段中第一个字节的序号，用于数据排序、去重、流量控制。</p><p>序列号是随机生成的，防止序列号猜测攻击。</p><h3 id="确认应答" tabindex="-1">确认应答 <a class="header-anchor" href="#确认应答" aria-label="Permalink to “确认应答”">​</a></h3><p>接收方收到数据后发送 ACK，确认号是期望收到的下一个字节的序号。ACK 是累积确认，表示之前的字节都已正确接收。</p><h3 id="超时重传" tabindex="-1">超时重传 <a class="header-anchor" href="#超时重传" aria-label="Permalink to “超时重传”">​</a></h3><p>发送方发送数据后启动定时器，如果超时未收到 ACK，则重传数据。超时时间根据 RTT（Round Trip Time，往返时间）动态调整，RTT 变化大时超时时间也变化大。</p><h3 id="快速重传" tabindex="-1">快速重传 <a class="header-anchor" href="#快速重传" aria-label="Permalink to “快速重传”">​</a></h3><p>接收方收到失序报文段后立即发送重复 ACK，发送方收到 3 个重复 ACK 后立即重传报文段，不必等待超时。</p><h3 id="选择性确认" tabindex="-1">选择性确认 <a class="header-anchor" href="#选择性确认" aria-label="Permalink to “选择性确认”">​</a></h3><p>SACK（Selective ACK）允许接收方告诉发送方哪些报文段已正确接收，哪些报文段丢失，发送方只需重传丢失的报文段，提高效率。</p><h2 id="tcp-流量控制" tabindex="-1">TCP 流量控制 <a class="header-anchor" href="#tcp-流量控制" aria-label="Permalink to “TCP 流量控制”">​</a></h2><h3 id="滑动窗口" tabindex="-1">滑动窗口 <a class="header-anchor" href="#滑动窗口" aria-label="Permalink to “滑动窗口”">​</a></h3><p>接收方通过窗口大小告诉发送方自己可以接收多少数据，发送方根据窗口大小调整发送速率，避免淹没接收方。</p><p>窗口大小是接收缓冲区的剩余空间，接收方处理数据后窗口增大，接收方缓冲区满时窗口为 0。</p><h3 id="零窗口问题" tabindex="-1">零窗口问题 <a class="header-anchor" href="#零窗口问题" aria-label="Permalink to “零窗口问题”">​</a></h3><p>接收方窗口为 0 时，发送方停止发送数据，定期发送零窗口探测报文段，询问窗口是否打开。</p><p>接收方窗口打开后发送 ACK，通知发送方可以继续发送数据。</p><h2 id="tcp-拥塞控制" tabindex="-1">TCP 拥塞控制 <a class="header-anchor" href="#tcp-拥塞控制" aria-label="Permalink to “TCP 拥塞控制”">​</a></h2><h3 id="慢启动" tabindex="-1">慢启动 <a class="header-anchor" href="#慢启动" aria-label="Permalink to “慢启动”">​</a></h3><p>连接建立后，拥塞窗口从 1 个 MSS 开始，每收到一个 ACK，拥塞窗口增加 1 个 MSS，指数增长。慢启动阈值（ssthresh）是慢启动和拥塞避免的分界线，拥塞窗口超过 ssthresh 后进入拥塞避免。</p><h3 id="拥塞避免" tabindex="-1">拥塞避免 <a class="header-anchor" href="#拥塞避免" aria-label="Permalink to “拥塞避免”">​</a></h3><p>拥塞窗口每 RTT 增加 1 个 MSS，线性增长，避免拥塞。</p><h3 id="快速重传-1" tabindex="-1">快速重传 <a class="header-anchor" href="#快速重传-1" aria-label="Permalink to “快速重传”">​</a></h3><p>收到 3 个重复 ACK 后，立即重传报文段，不必等待超时。</p><h3 id="快速恢复" tabindex="-1">快速恢复 <a class="header-anchor" href="#快速恢复" aria-label="Permalink to “快速恢复”">​</a></h3><p>快速重传后，不进入慢启动，而是将拥塞窗口设置为 ssthresh 的一半，进入拥塞避免。</p><p>TCP 是互联网的核心协议，理解 TCP 的连接建立、连接终止、可靠传输、流量控制、拥塞控制，有助于理解网络编程的底层原理。</p>',73)])])}const P=e(l,[["render",t]]);export{b as __pageData,P as default};
