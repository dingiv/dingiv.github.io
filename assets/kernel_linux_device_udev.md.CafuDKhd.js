import{_ as e,o as a,c as i,ah as d}from"./chunks/framework.D-EW4Mou.js";const r=JSON.parse('{"title":"udev","description":"","frontmatter":{"title":"udev","order":60},"headers":[],"relativePath":"kernel/linux/device/udev.md","filePath":"kernel/linux/device/udev.md"}'),n={name:"kernel/linux/device/udev.md"};function t(l,s,p,h,o,c){return a(),i("div",null,[...s[0]||(s[0]=[d(`<h1 id="udev-设备管理" tabindex="-1">udev 设备管理 <a class="header-anchor" href="#udev-设备管理" aria-label="Permalink to “udev 设备管理”">​</a></h1><p>udev 是 Linux 系统的设备管理器，负责管理 <code>/dev</code> 目录下的设备节点，以及处理用户空间中的设备事件。当系统添加或移除硬件设备时，内核会通过 netlink 机制通知 udev，udev 根据预定义的规则执行相应的动作，如创建设备节点、设置权限、加载模块、运行脚本等。</p><h2 id="从-devfs-到-udev-的演进" tabindex="-1">从 devfs 到 udev 的演进 <a class="header-anchor" href="#从-devfs-到-udev-的演进" aria-label="Permalink to “从 devfs 到 udev 的演进”">​</a></h2><p>在 udev 出现之前，Linux 使用 devfs 来管理设备节点。devfs 在内核空间运行，设备加入时自动在 <code>/dev</code> 目录下创建对应的设备文件。devfs 的问题在于设备节点命名不灵活，主次设备号分配混乱，而且内核态的代码难以维护。</p><p>udev 的设计将设备管理从内核空间移到用户空间，内核只负责检测设备变化和发送事件，用户空间的 udevd 守护进程负责处理这些事件。这种分离设计使得设备管理策略可以通过配置文件灵活调整，无需重新编译内核。</p><h2 id="udev-的工作机制" tabindex="-1">udev 的工作机制 <a class="header-anchor" href="#udev-的工作机制" aria-label="Permalink to “udev 的工作机制”">​</a></h2><p>udev 系统由三个核心组件构成：内核的 sysfs 文件系统、内核的 netlink 事件机制、用户空间的 udevd 守护进程。</p><p>sysfs 是一个虚拟文件系统，挂载在 <code>/sys</code> 目录下，它将内核中的设备模型以文件和目录的形式暴露给用户空间。每个设备在 sysfs 中都有一个对应的目录，包含设备的属性信息。例如，<code>/sys/block/sda</code> 目录包含了 sda 硬盘的各种属性，如大小、型号、序列号等。</p><p>当系统检测到设备变化时，内核会通过 netlink socket 发送 uevent 事件到用户空间。这些事件包含设备的动作（add/remove/change）、设备路径、以及一些关键属性。udevd 守护进程监听这些事件，并根据规则库决定如何响应。</p><p>udevd 收到事件后，会按优先级顺序处理规则文件。规则文件位于 <code>/etc/udev/rules.d/</code> 和 <code>/lib/udev/rules.d/</code> 目录，文件名以数字开头，数字越小优先级越高。每条规则由匹配键和赋值键组成，匹配键用于筛选事件，赋值键用于指定动作。</p><h2 id="udev-规则语法" tabindex="-1">udev 规则语法 <a class="header-anchor" href="#udev-规则语法" aria-label="Permalink to “udev 规则语法”">​</a></h2><p>udev 规则的语法简洁但功能强大。一条规则可以包含多个匹配条件，所有条件都满足时才执行相应的动作。匹配键包括 <code>KERNEL</code>（内核设备名）、<code>SUBSYSTEM</code>（子系统类型）、<code>ATTRS</code>（设备属性）、<code>DRIVERS</code>（驱动名称）等。赋值键包括 <code>NAME</code>（设备节点名）、<code>SYMLINK</code>（符号链接）、<code>OWNER</code>（所有者）、<code>GROUP</code>（所属组）、<code>MODE</code>（权限模式）、<code>RUN</code>（执行程序）等。</p><p>一个典型的 udev 规则示例：</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span># 为特定 USB 设备创建固定命名的符号链接</span></span>
<span class="line"><span>SUBSYSTEM==&quot;tty&quot;, ATTRS{idVendor}==&quot;0403&quot;, ATTRS{idProduct}==&quot;6001&quot;, SYMLINK+=&quot;ftdi_device&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span># 为 U 盘设置权限和所有者</span></span>
<span class="line"><span>SUBSYSTEM==&quot;block&quot;, ATTRS{idVendor}==&quot;0951&quot;, ATTRS{idProduct}==&quot;1666&quot;, OWNER=&quot;user&quot;, GROUP=&quot;storage&quot;, MODE=&quot;0660&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span># 设备添加时执行自定义脚本</span></span>
<span class="line"><span>ACTION==&quot;add&quot;, SUBSYSTEM==&quot;net&quot;, RUN+=&quot;/usr/local/bin/configure-network.sh %k&quot;</span></span></code></pre></div><p>规则中的 <code>==</code> 表示匹配，<code>+=</code> 表示追加，<code>=</code> 表示赋值。<code>%k</code> 是内核提供的设备名替换变量，其他常用变量包括 <code>%n</code>（内核设备号）、<code>%p</code>（设备路径）、<code>%m</code>（总线主编号）等。</p><h2 id="设备持久化命名" tabindex="-1">设备持久化命名 <a class="header-anchor" href="#设备持久化命名" aria-label="Permalink to “设备持久化命名”">​</a></h2><p>udev 最重要的功能之一是为设备提供持久化的命名。传统的设备命名如 <code>/dev/sda</code> 会因为设备发现顺序不同而改变，U 盘先插入可能是 sdb，后插入可能是 sdc。这对于需要稳定设备路径的应用（如自动挂载配置、备份脚本）是很大的问题。</p><p>udev 通过设备的唯一属性来创建稳定的符号链接。这些属性包括设备的序列号、厂商 ID、产品 ID、路径等。例如，以下规则会基于磁盘的 ID 创建 <code>/dev/disk/by-id/</code> 目录下的符号链接：</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>SUBSYSTEM==&quot;block&quot;, ENV{ID_SERIAL_SHORT}==&quot;S1XWNSDB012345&quot;, SYMLINK+=&quot;disk/by-id/$env{ID_SERIAL_SHORT}&quot;</span></span></code></pre></div><p>现代发行版已经预置了大量规则，用户可以在 <code>/dev/disk/by-uuid/</code>、<code>/dev/disk/by-label/</code>、<code>/dev/disk/by-path/</code> 等目录下找到各种持久化命名的设备链接。</p><h2 id="网络设备命名" tabindex="-1">网络设备命名 <a class="header-anchor" href="#网络设备命名" aria-label="Permalink to “网络设备命名”">​</a></h2><p>udev 也负责网络设备的命名。传统的 eth0、eth1 命名方式因设备发现顺序不稳定而被 systemd 的可预测命名方案取代。新方案根据设备的物理位置、固件信息、MAC 地址等生成稳定的设备名，如 <code>enp3s0</code>（PCI 总线 3 插槽 0 的以太网设备）、<code>wlp2s0</code>（PCI 总线 2 插槽 0 的无线设备）。</p><p>如果需要恢复传统的 eth0 命名，可以通过 udev 规则实现：</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>SUBSYSTEM==&quot;net&quot;, ACTION==&quot;add&quot;, DRIVERS==&quot;?*&quot;, ATTR{address}==&quot;00:11:22:33:44:55&quot;, NAME=&quot;eth0&quot;</span></span></code></pre></div><p>或者在内核启动参数中添加 <code>net.ifnames=0</code> 来禁用可预测命名。</p><h2 id="udevadm-工具" tabindex="-1">udevadm 工具 <a class="header-anchor" href="#udevadm-工具" aria-label="Permalink to “udevadm 工具”">​</a></h2><p>udevadm 是 udev 的管理工具，提供了调试和信息查询功能。<code>udevadm info</code> 可以查询设备的属性信息：</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 查询 sda 设备的所有属性</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">udevadm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> info</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --attribute-walk</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --name=/dev/sda</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 查询某个设备节点的详细信息</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">udevadm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> info</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --query=all</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --name=/dev/sda</span></span></code></pre></div><p><code>udevadm monitor</code> 可以实时监控内核发送的 uevent 事件：</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 监控所有 uevent</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">udevadm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> monitor</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 只监控内核层事件</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">udevadm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> monitor</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --kernel</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 只监控 udev 层事件</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">udevadm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> monitor</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --udev</span></span></code></pre></div><p><code>udevadm test</code> 可以模拟设备事件并测试规则，而不实际执行动作：</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 测试添加 sda 时的规则处理</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">udevadm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /sys/block/sda</span></span></code></pre></div><p><code>udevadm control</code> 用于控制 udevd 守护进程：</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 重载规则配置</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">udevadm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> control</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --reload-rules</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 触发所有设备的事件</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">udevadm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> trigger</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 触发特定子系统的设备事件</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">udevadm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> trigger</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --subsystem-match=net</span></span></code></pre></div><h2 id="内核模块自动加载" tabindex="-1">内核模块自动加载 <a class="header-anchor" href="#内核模块自动加载" aria-label="Permalink to “内核模块自动加载”">​</a></h2><p>udev 还负责在检测到新设备时自动加载相应的内核模块。当内核检测到设备但缺少驱动时，会发送 uevent 事件，udevd 根据设备的 <code>MODALIAS</code> 属性查找匹配的内核模块并加载。</p><p>模块别名信息存储在 <code>/lib/modules/$(uname -r)/modules.alias</code> 文件中，这个文件在内核模块安装时由 depmod 工具生成。如果需要禁用某个设备的自动加载，可以在 <code>/etc/modprobe.d/</code> 目录下创建 blacklist 配置：</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span># 禁止自动加载 pcspkr 模块</span></span>
<span class="line"><span>blacklist pcspkr</span></span></code></pre></div><h2 id="与-systemd-的集成" tabindex="-1">与 systemd 的集成 <a class="header-anchor" href="#与-systemd-的集成" aria-label="Permalink to “与 systemd 的集成”">​</a></h2><p>在现代 Linux 系统中，udevd 已经被集成到 systemd 中，成为 systemd-udevd 服务。systemd 通过 udev 规则来管理设备，同时利用设备事件来触发服务启动。例如，当某个 USB 网卡插入时，systemd 可以自动启动网络配置服务；当某个存储设备插入时，可以自动启动备份服务。</p><p>这种设备事件驱动的服务启动机制，使得系统可以根据实际硬件情况动态调整服务状态，实现了更加智能和高效的资源管理。</p><h2 id="故障排查" tabindex="-1">故障排查 <a class="header-anchor" href="#故障排查" aria-label="Permalink to “故障排查”">​</a></h2><p>udev 相关的故障通常表现为设备节点未创建、权限不正确、规则不生效等问题。排查时首先查看 udevd 日志：</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">journalctl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -u</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> systemd-udevd</span></span></code></pre></div><p>使用 <code>udevadm monitor</code> 确认内核是否发送了设备事件，如果没有事件，问题可能在内核驱动层。如果有事件但规则未生效，使用 <code>udevadm test</code> 查看规则匹配过程，检查规则语法和优先级。</p><p>设备属性信息是编写规则的基础，使用 <code>udevadm info --attribute-walk</code> 可以获取设备的完整属性树，帮助找到稳定的匹配键。</p>`,46)])])}const u=e(n,[["render",t]]);export{r as __pageData,u as default};
