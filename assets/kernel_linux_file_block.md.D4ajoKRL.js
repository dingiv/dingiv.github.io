import{H as e,V as t,et as n,j as r}from"./chunks/framework.C_EfTa_6.js";var i=JSON.parse(`{"title":"块设备子系统","description":"","frontmatter":{},"headers":[],"relativePath":"kernel/linux/file/block.md","filePath":"kernel/linux/file/block.md"}`),a={name:`kernel/linux/file/block.md`};function o(r,i,a,o,s,c){return n(),t(`div`,null,[...i[0]||=[e(`<h1 id="块设备子系统" tabindex="-1">块设备子系统 <a class="header-anchor" href="#块设备子系统" aria-label="Permalink to “块设备子系统”">​</a></h1><p>块设备子系统是文件系统与磁盘驱动之间的中间层。文件系统负责在磁盘的字节数组上组织文件目录的逻辑结构，磁盘驱动负责与物理硬件交互完成数据读写，而块设备子系统屏蔽了底层硬件差异，向上为文件系统提供统一的 bio（Block I/O）请求接口，向下将请求分发给具体的磁盘驱动。同时，它还承担 I/O 调度、请求合并、队列管理等职责，是存储 I/O 路径上的核心枢纽。</p><h2 id="在存储栈中的位置" tabindex="-1">在存储栈中的位置 <a class="header-anchor" href="#在存储栈中的位置" aria-label="Permalink to “在存储栈中的位置”">​</a></h2><p>从上到下，一次文件读写的完整路径依次经过：应用程序通过系统调用进入 VFS → VFS 查找文件对应的 inode 和 dentry → 文件系统（如 ext4）根据 inode 中的逻辑块映射定位数据在磁盘分区中的物理位置 → 块设备子系统接收文件系统生成的 bio 请求，进行调度和合并 → 块设备驱动将请求转换为硬件命令提交给磁盘控制器 → 磁盘控制器通过 DMA 将数据传输到内存 → 中断通知完成 → 逐层回调返回。</p><ul><li>VFS</li><li>文件系统</li><li>块设备子系统</li><li>设备管理子系统 <ul><li>块设备驱动</li><li>块设备抽象</li><li>物理磁盘</li></ul></li></ul><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>应用程序</span></span>
<span class="line"><span>  ↓  open/read/write 系统调用</span></span>
<span class="line"><span>VFS（file → dentry → inode）</span></span>
<span class="line"><span>  ↓  inode-&gt;i_mapping 操作页面缓存</span></span>
<span class="line"><span>文件系统（ext4/xfs/...）</span></span>
<span class="line"><span>  ↓  逻辑块 → 物理扇区映射，生成 bio</span></span>
<span class="line"><span>块设备子系统</span></span>
<span class="line"><span>  ↓  I/O 调度、请求合并、分发</span></span>
<span class="line"><span>块设备驱动（NVMe/SCSI/...）</span></span>
<span class="line"><span>  ↓  硬件命令提交</span></span>
<span class="line"><span>磁盘控制器 → DMA → 物理磁盘</span></span></code></pre></div><p>在这个层次结构中，块设备子系统向上通过 <code>submit_bio</code> 接收 bio 请求，向下通过 <code>gendisk-&gt;fops</code> 或 request queue 与驱动交互。块设备子系统与<a href="./../device/">设备管理子系统</a>的衔接点是 <code>struct block_device</code> 和 <code>struct gendisk</code>：<code>block_device</code> 抽象磁盘分区（如 <code>/dev/sda1</code>），<code>gendisk</code> 抽象物理磁盘（如 <code>/dev/sda</code>），两者通过设备号关联。</p><h2 id="核心数据结构" tabindex="-1">核心数据结构 <a class="header-anchor" href="#核心数据结构" aria-label="Permalink to “核心数据结构”">​</a></h2><h3 id="bio" tabindex="-1">bio <a class="header-anchor" href="#bio" aria-label="Permalink to “bio”">​</a></h3><p>bio（Block I/O）是块设备子系统的基本请求单位，由文件系统构造，描述一次连续的块 I/O 操作。文件系统不直接调用磁盘驱动，而是构造 bio 并提交给块设备子系统，由子系统负责后续的调度和分发。</p><div class="language-c"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> bio {</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> bio </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bi_next;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">              /* 请求链表 */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> block_device </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bi_bdev;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">     /* 目标块设备 */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> short</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> bi_opf;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">            /* 操作类型（REQ_OP_READ/WRITE） */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> bvec_iter bi_iter;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">         /* 当前段迭代器 */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> bio_vec </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bi_io_vec;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">        /* 数据段向量（物理页 + 偏移 + 长度） */</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    bio_end_io_t</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bi_end_io;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">          /* 完成回调函数 */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    void</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bi_private;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">                 /* 回调私有数据 */</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    atomic_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> bi_remaining;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">            /* 剩余完成计数 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>bio 的核心字段是 <code>bi_io_vec</code>，它是一个分散-聚集（scatter-gather）向量数组，每个元素 <code>struct bio_vec</code> 描述一个物理内存页中的数据段（页指针 + 页内偏移 + 长度）。一次 bio 可以包含多个不连续的物理页，但它们在磁盘上的逻辑块地址必须是连续的。文件系统在构造 bio 时，根据文件在磁盘上的物理块分布来填充 <code>bi_io_vec</code>。</p><p>bio 的完成通知机制通过 <code>bi_end_io</code> 回调实现。当块设备子系统完成一次 bio 的所有物理页传输后，调用 <code>bi_end_io(bio)</code> 通知文件系统。文件系统在该回调中检查错误状态、释放资源，并可能唤醒等待该 I/O 的进程。</p><h3 id="request-与-request-queue" tabindex="-1">request 与 request_queue <a class="header-anchor" href="#request-与-request-queue" aria-label="Permalink to “request 与 request_queue”">​</a></h3><p>request 是 I/O 调度器对 bio 进行合并和排序后的产物。当 bio 进入块设备子系统后，调度器可能将多个 bio 合并为一个 request（如果它们访问的磁盘扇区是相邻或重叠的），也可能对多个 request 按磁盘扇区顺序排序以减少寻道时间。request 是提交给块设备驱动的最终请求单位。</p><div class="language-c"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> request {</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> request_queue </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">q;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">          /* 所属队列 */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cmd_flags;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">           /* 操作标志 */</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    sector_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __sector;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">                /* 起始扇区 */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> bio </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bio;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">                  /* bio 链表 */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> bio </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">biotail;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">              /* bio 链尾 */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> hlist_node hash;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">           /* 哈希表（查找合并候选） */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> request_queue {</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> elevator_queue </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">elevator;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">  /* I/O 调度器 */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> request </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">last_merge;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">       /* 最近合并的请求 */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> blk_queue_ctx </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">queue_ctx;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">  /* per-CPU 队列上下文 */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> queue_flags;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">         /* 队列标志 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    make_request_fn </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">make_request_fn;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> /* bio 处理函数 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>request_queue 是管理 request 的核心结构。传统上，每个 gendisk 关联一个 request_queue，所有发往该磁盘的 bio 先进入 queue，经调度器合并排序后再发给驱动。现代块层引入了 <strong>blk-mq（Block Multi-Queue）</strong> 架构，将单队列拆分为多级队列结构，以适应多核 CPU 和高速存储设备的需求。</p><h3 id="block-device-与-gendisk" tabindex="-1">block_device 与 gendisk <a class="header-anchor" href="#block-device-与-gendisk" aria-label="Permalink to “block_device 与 gendisk”">​</a></h3><p><code>struct block_device</code> 和 <code>struct gendisk</code> 是块设备子系统中对上层的抽象。它们对应了块设备在 VFS 层和设备管理层中的表示。</p><div class="language-c"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// 抽象磁盘分区（如 /dev/sda1）</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> block_device {</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    dev_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> bd_dev;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">                     /* 设备号（major:minor） */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> gendisk </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bd_disk;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">          /* 所属物理磁盘 */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> inode </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bd_inode;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">           /* VFS inode（/dev/sda1 的 inode） */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> block_device </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bd_contains;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> /* 父设备（sda1 → sda） */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> hd_struct </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bd_part;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">        /* 分区信息 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// 抽象物理磁盘（如 /dev/sda）</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> gendisk {</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> major;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">                        /* 主设备号 */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> first_minor;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">                  /* 起始次设备号 */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> minors;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">                       /* 次设备号数量（分区数+1） */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    char</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;"> disk_name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[DISK_NAME_LEN];</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">    /* 设备名（&quot;sda&quot;） */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> block_device_operations </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fops;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> /* 驱动操作接口 */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> request_queue </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">queue;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">      /* 请求队列 */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> disk_part_tbl </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">part_tbl;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">   /* 分区表 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>gendisk 是设备管理层中块设备驱动的注册产物。驱动在 probe 中分配并初始化 gendisk，通过 <code>add_disk</code> 注册到块设备子系统，内核自动在 <code>/sys/block/</code> 下创建对应条目，并通知 udev 创建 <code>/dev/sdX</code> 设备节点。block_device 是内核在首次打开某个块设备文件时创建的，一个 gendisk 对应一个 block_device（整个磁盘），每个分区对应一个额外的 block_device（通过 <code>bd_contains</code> 指向整个磁盘的 block_device）。</p><p>在 VFS 层，块设备的 inode 通过 <code>i_bdev</code> 指向 block_device，block_device 的 <code>bd_disk</code> 指向 gendisk，gendisk 的 <code>fops</code> 指向驱动操作函数。这条链路将 VFS、块设备子系统和设备驱动连接起来。block_device 对标了隔壁网络设备中的 socket 结构，是 VFS 层操作下层的 API 抽象。</p><h2 id="blk-mq-架构" tabindex="-1">blk-mq 架构 <a class="header-anchor" href="#blk-mq-架构" aria-label="Permalink to “blk-mq 架构”">​</a></h2><p>现代 Linux 内核的块设备子系统采用 <strong>blk-mq（Block Multi-Queue）</strong> 架构，取代了早期的单队列架构。单队列架构下所有 CPU 核心共享一个 request_queue 和一把自旋锁，多核并发 I/O 时锁竞争严重，成为性能瓶颈。blk-mq 通过多级队列设计解决了这个问题。</p><p>blk-mq 将 I/O 路径分为两级队列：</p><p><strong>软件阶段（Software Stage）</strong>：每个 CPU 核心拥有独立的<strong>提交队列（Submission Queue, SQ）</strong>。文件系统提交 bio 时，通过 per-CPU 的映射将 bio 放入当前 CPU 的 SQ 中。SQ 无锁操作，多核之间不存在竞争，这是 blk-mq 性能提升的关键。</p><p><strong>硬件阶段（Hardware Stage）</strong>：一组<strong>硬件队列（Hardware Queue, HW Queue）</strong> 与底层硬件的实际并行能力对应。NVMe 设备可能支持 64 个或更多的提交/完成队列，每个队列可以绑定不同的 CPU 核。blk-mq 将 SQ 中的 request 映射到 HW Queue，由硬件队列对应的驱动上下文处理。</p><p>两级队列之间的映射关系通过 <code>blk_mq_tag_set</code> 中的 <code>map</code> 数组配置。简单的设备可以设置 1:1 映射（SQ 直接对应 HW Queue），复杂的设备可以 N:1 映射（多个 SQ 共享一个 HW Queue）。驱动在初始化时通过 <code>blk_mq_alloc_tag_set</code> 和 <code>blk_mq_init_queue</code> 创建 tag set 和 request queue。</p><div class="language-c"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// 块设备驱动初始化 blk-mq 的典型流程</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> blk_mq_tag_set tag_set </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .ops </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> &amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">my_mq_ops,</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">           // 驱动实现的 mq 操作</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .nr_hw_queues </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">           // 硬件队列数量</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .queue_depth </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 128</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">          // 每个队列的深度</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .numa_node </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> NUMA_NO_NODE,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .cmd_size </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> sizeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> my_cmd),</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> // 驱动私有数据大小</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">blk_mq_alloc_tag_set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">tag_set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> request_queue </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">queue </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> blk_mq_init_queue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">tag_set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// 驱动实现的 mq 操作</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> blk_mq_ops my_mq_ops </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .queue_rq </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> my_queue_rq,</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">     // 处理 request</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .complete </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> my_complete,</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">     // 请求完成回调</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .init_hctx </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> my_init_hctx,</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">   // 初始化硬件队列上下文</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p><code>queue_rq</code> 是驱动最核心的回调，驱动在此将 request 转换为硬件命令（如 NVMe 提交队列条目、SCSI CDB），通过 MMIO 或 DMA 提交给磁盘控制器。<code>complete</code> 在硬件完成 I/O 后由中断处理程序调用，驱动在此回收资源并通知块设备子系统该 request 已完成。</p><h2 id="i-o-调度" tabindex="-1">I/O 调度 <a class="header-anchor" href="#i-o-调度" aria-label="Permalink to “I/O 调度”">​</a></h2><p>I/O 调度器的目标是将无序的 I/O 请求整理为有利于磁盘执行的顺序，主要优化两个指标：减少机械硬盘的磁头寻道时间（对于 SSD，寻道时间不存在，但仍有其他优化空间），提高吞吐量。</p><p>对于机械硬盘（HDD），I/O 调度器通过将请求按扇区顺序排序来最小化磁头移动距离。经典的调度算法包括 <strong>Deadline</strong>（保证每个请求的最长等待时间不超过阈值，防止饥饿）和 <strong>CFQ</strong>（Completely Fair Queuing，为每个进程分配独立的队列，按时间片轮转服务）。现代内核中 CFQ 已被 <strong>BFQ</strong>（Budget Fair Queueing）取代，BFQ 在 CFQ 基础上引入了带宽预算机制，对交互式应用（如桌面视频播放）的延迟控制更好。</p><p>对于 SSD 和 NVMe 设备，寻道时间的概念不再适用，随机读写和顺序读写的性能差异远小于 HDD。此时调度器的核心工作从排序转向合并和限制队列深度。<strong>mq-deadline</strong> 是 blk-mq 架构下 Deadline 调度器的多队列版本，适用于大多数场景。<strong>none</strong>（Noop）是最简单的调度器，不做排序和合并，直接将请求转发给驱动，适用于自带队列管理的 NVMe 设备。内核会根据设备类型自动选择合适的调度器，也可以通过 <code>/sys/block/sdX/queue/scheduler</code> 手动切换。</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span># 查看和切换 I/O 调度器</span></span>
<span class="line"><span>cat /sys/block/sda/queue/scheduler</span></span>
<span class="line"><span>echo mq-deadline &gt; /sys/block/sda/queue/scheduler</span></span></code></pre></div><h2 id="i-o-路径详解" tabindex="-1">I/O 路径详解 <a class="header-anchor" href="#i-o-路径详解" aria-label="Permalink to “I/O 路径详解”">​</a></h2><p>以 ext4 文件系统读取一个文件为例，完整追踪 I/O 从用户态到磁盘的路径。</p><h3 id="读操作" tabindex="-1">读操作 <a class="header-anchor" href="#读操作" aria-label="Permalink to “读操作”">​</a></h3><p>用户调用 <code>read(fd, buf, count)</code> 进入内核。VFS 通过 fd 找到 <code>struct file</code>，再通过 <code>file-&gt;f_inode</code> 找到 inode。VFS 检查页面缓存（<code>inode-&gt;i_mapping</code>），如果数据已在缓存中（缓存命中），直接通过 <code>copy_to_user</code> 将数据从内核页拷贝到用户缓冲区，I/O 在此结束。</p><p>缓存未命中时，VFS 调用 <code>address_space-&gt;a_ops-&gt;readpage</code>（由 ext4 实现）。ext4 的 readpage 函数首先根据文件逻辑块号查找 ext4 的 extent 映射（<code>struct extent_map</code>），将文件的逻辑块号转换为磁盘分区的物理扇区号。然后构造 bio：<code>bio-&gt;bi_bdev</code> 指向分区的 block_device，<code>bi_iter.bi_sector</code> 设为起始物理扇区，<code>bi_io_vec</code> 填入目标内存页，<code>bi_end_io</code> 设为完成回调。构造完成后调用 <code>submit_bio(bio)</code> 将 bio 提交给块设备子系统。</p><p>块设备子系统接收到 bio 后，根据 gendisk 关联的 request_queue 进入 blk-mq 路径。bio 被放入当前 CPU 的提交队列，调度器决定是否与其他 request 合并。随后 request 进入硬件队列，驱动的 <code>queue_rq</code> 被调用。NVMe 驱动将 request 中的扇区号、长度、数据缓冲区 DMA 地址等信息填入 NVMe 提交队列条目，向控制器门铃寄存器（doorbell）写入提交计数，通知硬件处理。</p><p>NVMe 控制器通过 DMA 将数据从磁盘读取到 bio 指定的内存页中，完成后发送中断。驱动在中断处理中调用 <code>blk_mq_complete_request</code>，块设备子系统调用 bio 的 <code>bi_end_io</code>，ext4 的完成回调唤醒等待该页的进程。最终数据已在页面缓存中，后续的 <code>copy_to_user</code> 将数据拷贝到用户缓冲区。</p><h3 id="写操作" tabindex="-1">写操作 <a class="header-anchor" href="#写操作" aria-label="Permalink to “写操作”">​</a></h3><p>写操作的路径与读操作类似，但有一个关键差异：数据先写入页面缓存（延迟写）。用户调用 <code>write(fd, buf, count)</code> 时，VFS 将数据从用户缓冲区 <code>copy_from_user</code> 到内核页面缓存中的目标页，并将该页标记为脏页（<code>SetPageDirty</code>），write 系统调用立即返回。</p><p>脏页不会立刻写回磁盘，而是由内核的回写机制（writeback）在适当时机触发。触发条件包括：脏页比例超过阈值（<code>vm.dirty_ratio</code>）、内存紧张需要回收页面、应用程序主动调用 <code>fsync</code> 或 <code>fdatasync</code>、或者定时器周期性触发（kworker 线程）。</p><p>回写时，<code>address_space-&gt;a_ops-&gt;writepages</code>（ext4 实现）被调用，ext4 将脏页构造为 WRITE 类型的 bio 提交到块设备子系统。对于日志文件系统（如 ext4），在写数据之前还需要先将元数据变更写入日志（journal），日志写入和数据写入各自生成独立的 bio，但日志 bio 必须先于数据 bio 完成以保证崩溃一致性。</p><h3 id="direct-i-o-与-sync-i-o" tabindex="-1">Direct I/O 与 Sync I/O <a class="header-anchor" href="#direct-i-o-与-sync-i-o" aria-label="Permalink to “Direct I/O 与 Sync I/O”">​</a></h3><p>默认的 I/O 路径经过页面缓存，适合大多数场景。但在某些场景下需要绕过页面缓存：</p><p><strong>Direct I/O（O_DIRECT）</strong>：用户调用 <code>open</code> 时指定 <code>O_DIRECT</code> 标志，I/O 数据直接在用户缓冲区和磁盘之间传输，不经过页面缓存。适用于数据库等自行管理缓存的场景，避免双重缓存（数据库 buffer pool + 内核页面缓存）浪费内存。Direct I/O 要求用户缓冲区的地址和大小必须对齐到文件系统逻辑块大小（通常 4KB），否则系统调用返回 EINVAL。</p><p><strong>Sync I/O（O_SYNC）</strong>：每次 write 系统调用在数据写入磁盘后才返回，不依赖回写机制的延迟。适用于对数据持久性要求极高的场景（如金融交易日志），但性能开销较大。</p><p>两者可以组合使用（<code>O_DIRECT | O_SYNC</code>），此时数据直接从用户缓冲区 DMA 到磁盘，write 在硬件确认完成后返回。</p><h2 id="分区管理" tabindex="-1">分区管理 <a class="header-anchor" href="#分区管理" aria-label="Permalink to “分区管理”">​</a></h2><p>一块物理磁盘可以被划分为多个分区，每个分区在内核中对应一个 <code>struct hd_struct</code> 和一个 <code>struct block_device</code>。分区表存储在磁盘的第一个扇区（LBA 0）中，常见格式有 <strong>MBR</strong>（Master Boot Record，最多 4 个主分区，支持扩展分区）和 <strong>GPT</strong>（GUID Partition Table，最多 128 个分区，支持 UEFI 启动）。</p><p>内核在块设备注册后（<code>add_disk</code>），会自动读取磁盘的分区表，为每个分区创建 block_device 和对应的 <code>/dev/sdXN</code> 设备节点。分区信息存储在 <code>gendisk-&gt;part_tbl</code> 中。分区调整（如 <code>fdisk</code>、<code>parted</code>）后，需要通知内核重新读取分区表（通过 <code>ioctl(BLKRRPART)</code> 或重新插拔设备）。</p><p>分区在 I/O 路径上的体现是扇区偏移量的转换。当文件系统向分区 <code>/dev/sda1</code> 提交 bio 时，bio 中的扇区号是相对于分区起始位置的。块设备子系统在将 bio 分发给驱动之前，会将扇区号加上分区的起始偏移量，转换为相对于整块磁盘的绝对扇区号。这个转换由 <code>blk_partition_remap</code> 函数完成，对文件系统和驱动都是透明的。</p>`,55)]])}var s=r(a,[[`render`,o]]);export{i as __pageData,s as default};