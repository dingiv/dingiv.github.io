import{_ as i,o as a,c as e,ah as n}from"./chunks/framework.BNo2o5Ln.js";const c=JSON.parse('{"title":"性能调优实践","description":"","frontmatter":{"title":"性能调优实践","order":1},"headers":[],"relativePath":"server/db/operations/tuning.md","filePath":"server/db/operations/tuning.md"}'),h={name:"server/db/operations/tuning.md"};function l(t,s,p,k,d,r){return a(),e("div",null,[...s[0]||(s[0]=[n(`<h1 id="性能调优实践" tabindex="-1">性能调优实践 <a class="header-anchor" href="#性能调优实践" aria-label="Permalink to “性能调优实践”">​</a></h1><p>数据库性能调优是一个系统工程，需要从 SQL 优化、索引设计、参数配置、架构调整等多个层面入手。本节介绍实用的调优方法和工具。</p><h2 id="慢查询分析" tabindex="-1">慢查询分析 <a class="header-anchor" href="#慢查询分析" aria-label="Permalink to “慢查询分析”">​</a></h2><h3 id="开启慢查询日志" tabindex="-1">开启慢查询日志 <a class="header-anchor" href="#开启慢查询日志" aria-label="Permalink to “开启慢查询日志”">​</a></h3><p>慢查询日志是定位性能问题的首要工具，它记录执行时间超过阈值的 SQL 语句。MySQL 中通过 <code>slow_query_log</code> 和 <code>long_query_time</code> 参数控制。</p><div class="language-sql"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- 开启慢查询日志</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SET</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> GLOBAL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> slow_query_log </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ON</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SET</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> GLOBAL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> long_query_time </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- 超过1秒的查询被记录</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SET</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> GLOBAL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> log_queries_not_using_indexes </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ON</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- 记录未使用索引的查询</span></span></code></pre></div><p>慢查询日志默认存储在数据目录，可以通过 <code>slow_query_log_file</code> 参数指定路径。日志格式包含查询时间、锁时间、返回行数、扫描行数等关键信息，这些数据是分析瓶颈的依据。</p><h3 id="分析慢查询日志" tabindex="-1">分析慢查询日志 <a class="header-anchor" href="#分析慢查询日志" aria-label="Permalink to “分析慢查询日志”">​</a></h3><p>pt-query-digest 是 Percona Toolkit 提供的慢查询分析工具，它能从大量日志中提取有价值的信息。工具会统计每个查询的执行次数、总执行时间、平均执行时间、返回行数、扫描行数，并按照影响排序。</p><p>分析慢查询时重点关注三个指标：执行次数、平均执行时间、扫描行数与返回行数的比值。执行次数高的查询即使单次快，总体影响也可能很大；平均执行时间长的查询需要优化；扫描行数远大于返回行数说明索引效率低，这是最典型的优化点。</p><h3 id="explain-分析执行计划" tabindex="-1">EXPLAIN 分析执行计划 <a class="header-anchor" href="#explain-分析执行计划" aria-label="Permalink to “EXPLAIN 分析执行计划”">​</a></h3><p>EXPLAIN 显示 MySQL 如何执行查询，是诊断 SQL 性能的核心工具。输出的 <code>type</code> 字段表示访问类型，从最好到最差依次是：const、eq_ref、ref、range、index、ALL。const 表示通过主键或唯一索引定位单行，eq_ref 表示连接时使用主键或唯一索引，ref 表示非唯一索引等值查询，range 表示范围扫描，index 表示索引扫描，ALL 表示全表扫描。</p><p><code>key</code> 字段显示实际使用的索引，<code>key_len</code> 显示使用的索引字节数，可以判断是否使用了复合索引的全部列。<code>rows</code> 字段是估算的扫描行数，越小越好。<code>Extra</code> 字段的 Using index 表示覆盖索引，Using filesort 表示需要文件排序，Using temporary 表示使用临时表，这两个 Extra 通常意味着需要优化。</p><h2 id="索引诊断" tabindex="-1">索引诊断 <a class="header-anchor" href="#索引诊断" aria-label="Permalink to “索引诊断”">​</a></h2><h3 id="索引使用情况分析" tabindex="-1">索引使用情况分析 <a class="header-anchor" href="#索引使用情况分析" aria-label="Permalink to “索引使用情况分析”">​</a></h3><p>MySQL 的 <code>performance_schema</code> 数据库包含 <code>table_io_waits_summary_by_index_usage</code> 表，记录了每个索引的使用情况。通过查询该表可以找出从未使用的索引，这些索引占用了存储空间和写入开销，可以安全删除。</p><div class="language-sql"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SELECT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> object_schema, object_name, index_name, count_star</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">FROM</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> performance_schema</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">table_io_waits_summary_by_index_usage</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">WHERE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> index_name </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">IS NOT NULL</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AND</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> count_star </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AND</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> index_name </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;PRIMARY&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ORDER BY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> object_schema, object_name;</span></span></code></pre></div><p>索引选择性是衡量索引质量的重要指标，计算公式是 <code>COUNT(DISTINCT column) / COUNT(*)</code>。选择性越接近 1，索引效率越高。对于选择性低的列（如性别、状态），索引效果很差，不适合单独建索引。</p><h3 id="索引失效场景" tabindex="-1">索引失效场景 <a class="header-anchor" href="#索引失效场景" aria-label="Permalink to “索引失效场景”">​</a></h3><p>索引列上使用函数会导致索引失效，例如 <code>WHERE DATE(created_at) = &#39;2023-01-01&#39;</code> 无法使用 created_at 上的索引。解决方案是将函数移到等号右侧，改写为 <code>WHERE created_at &gt;= &#39;2023-01-01&#39; AND created_at &lt; &#39;2023-01-02&#39;</code>。</p><p>隐式类型转换也会导致索引失效。例如列类型是 VARCHAR，查询条件用整数 <code>WHERE phone = 13800138000</code>，MySQL 会进行类型转换，导致无法使用索引。解决方案是将查询条件改为字符串形式。</p><p>LIKE 查询的前缀通配符会导致索引失效，<code>WHERE name LIKE &#39;%张%&#39;</code> 无法使用索引。如果业务需要模糊搜索，可以考虑全文索引或 Elasticsearch。对于后缀通配符 <code>WHERE name LIKE &#39;张%&#39;</code>，索引仍然有效。</p><h3 id="索引设计建议" tabindex="-1">索引设计建议 <a class="header-anchor" href="#索引设计建议" aria-label="Permalink to “索引设计建议”">​</a></h3><p>为 WHERE、JOIN、ORDER BY、GROUP BY 子句中的列创建索引，但不要为每个查询都创建索引，需要权衡查询频率和写入开销。高并发写入的表应该少建索引，读多写少的表可以多建索引。</p><p>复合索引的列顺序遵循最左前缀原则，将区分度高的列放在前面。区分度可以用 <code>COUNT(DISTINCT column) / COUNT(*)</code> 计算，值越高区分度越好。对于等值查询后跟范围查询的场景，等值列放在前面，例如索引 (status, created_at) 可以支持 <code>WHERE status = 1 AND created_at &gt; &#39;2023-01-01&#39;</code>。</p><p>覆盖索引可以避免回表，极大提升查询性能。例如对于查询 <code>SELECT name FROM users WHERE email = ?</code>，索引 (email, name) 可以直接从索引获取 name，不需要回表查询完整记录。</p><h2 id="参数配置优化" tabindex="-1">参数配置优化 <a class="header-anchor" href="#参数配置优化" aria-label="Permalink to “参数配置优化”">​</a></h2><h3 id="连接数配置" tabindex="-1">连接数配置 <a class="header-anchor" href="#连接数配置" aria-label="Permalink to “连接数配置”">​</a></h3><p><code>max_connections</code> 控制最大连接数，默认值 151 对生产环境偏小。设置过小会导致连接拒绝错误，设置过大会导致内存不足。估算公式是 <code>max_connections = (可用内存 - Global Buffer) / 每个线程的私有内存</code>。每个线程的私有内存包括 sort_buffer、join_buffer、read_buffer、read_rnd_buffer 等，默认都是 256KB，可以根据实际情况调整。</p><p>连接池可以缓解连接数压力。应用层使用 HikariCP、Druid 等连接池，避免频繁创建和销毁连接。对于短连接应用，考虑使用线程池模式（MySQL Enterprise 版本或 Percona 版本支持）。</p><h3 id="缓冲池配置" tabindex="-1">缓冲池配置 <a class="header-anchor" href="#缓冲池配置" aria-label="Permalink to “缓冲池配置”">​</a></h3><p><code>innodb_buffer_pool_size</code> 是 InnoDB 最重要的参数，控制缓冲池大小。缓冲池缓存数据和索引页，设置为可用内存的 50%-80% 是合理范围。对于专用数据库服务器，可以设置为 70%-80%；对于与应用混合部署的服务器，设置为 50% 左右。</p><p><code>innodb_buffer_pool_instances</code> 控制缓冲池实例数，设置为 (innodb_buffer_pool_size / 1GB) 是经验法则。多个实例可以减少锁竞争，提升并发性能。</p><p><code>innodb_log_file_size</code> 控制 redo log 文件大小，默认 48MB 偏小。设置过小会导致频繁切换日志，设置过大会导致崩溃恢复耗时。推荐设置为 256MB-2GB，根据写入量调整。写入量大的场景可以设置更大，但要注意 <code>innodb_log_buffer_size</code> 也要相应增加，一般为 16MB-64MB。</p><h3 id="io-相关配置" tabindex="-1">IO 相关配置 <a class="header-anchor" href="#io-相关配置" aria-label="Permalink to “IO 相关配置”">​</a></h3><p><code>innodb_io_capacity</code> 和 <code>innodb_io_capacity_max</code> 控制脏页刷盘速率。对于 SSD，设置为 2000-40000；对于机械硬盘，设置为 200-2000。设置过小会导致脏页积压，设置过大会影响业务 I/O。可以通过观察 <code>innodb_buffer_pool_wait_free</code> 状态判断是否合理，如果该值增长说明缓冲池不足，刷盘跟不上。</p><p><code>innodb_flush_method</code> 控制刷盘方式。Linux 下推荐 <code>O_DIRECT</code>，避免双缓冲，减少内存拷贝。Windows 下只能使用 <code>unbuffered</code>。</p><p><code>innodb_flush_log_at_trx_commit</code> 控制事务提交时的刷盘策略。1 表示每次提交都刷盘（最安全但最慢），2 表示每次提交写到操作系统缓存，每秒刷盘一次（折中方案），0 表示每秒写日志并刷盘（最快但不安全）。生产环境推荐设置为 1，对于可以容忍少量数据丢失的场景可以设置为 2。</p><h2 id="查询优化技巧" tabindex="-1">查询优化技巧 <a class="header-anchor" href="#查询优化技巧" aria-label="Permalink to “查询优化技巧”">​</a></h2><h3 id="分页优化" tabindex="-1">分页优化 <a class="header-anchor" href="#分页优化" aria-label="Permalink to “分页优化”">​</a></h3><p>传统分页 <code>LIMIT offset, size</code> 在 offset 很大时性能很差，因为需要扫描 offset 条记录后才开始返回数据。优化方案有三种：第一种是记住上一页的最大 ID，使用 <code>WHERE id &gt; last_id LIMIT size</code>，这要求 ID 连续且可排序；第二种是使用延迟关联，先通过覆盖索引获取 ID，再 JOIN 查询完整记录；第三种是使用覆盖索引 + 自连接，适用于复杂排序场景。</p><div class="language-sql"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- 传统分页（offset 大时性能差）</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SELECT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> FROM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ORDER BY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">LIMIT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1000000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- 记住上一页最大 ID（推荐）</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SELECT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> FROM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">WHERE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1000000</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ORDER BY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">LIMIT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- 延迟关联（适用于复杂排序）</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SELECT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> o.</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> FROM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders o</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">INNER JOIN</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SELECT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">FROM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ORDER BY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> created_at </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">LIMIT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1000000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) tmp</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ON</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> o</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> tmp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><h3 id="join-优化" tabindex="-1">JOIN 优化 <a class="header-anchor" href="#join-优化" aria-label="Permalink to “JOIN 优化”">​</a></h3><p>多表 JOIN 时，小表驱动大表是基本原则。优化器会自动选择驱动表，但统计信息不准确时可能选择错误。可以通过 STRAIGHT_JOIN 强制连接顺序，但需要确保连接条件有索引。</p><p>子查询在某些场景下可以转换为 JOIN，性能更好。MySQL 5.6+ 对子查询做了优化，但仍然建议优先使用 JOIN。对于 IN 子查询，如果外层表大、内层表小，可以考虑转换为 EXISTS。</p><h3 id="批量操作" tabindex="-1">批量操作 <a class="header-anchor" href="#批量操作" aria-label="Permalink to “批量操作”">​</a></h3><p>批量插入比单条插入快得多，主要优势是减少了 SQL 解析开销和网络往返次数。使用 <code>INSERT INTO t VALUES (...), (...), (...)</code> 一次插入多条记录。对于大批量导入，使用 <code>LOAD DATA INFILE</code> 比 INSERT 快 10-20 倍。</p><p>批量更新和删除同样有性能优势。一次性更新多条记录时，使用 CASE WHEN 或 JOIN 批量更新，避免逐条更新。批量删除时，使用 <code>WHERE id IN (...)</code> 比 <code>WHERE id = ?</code> 多次执行更高效。</p><p>数据库性能调优是持续的过程，建立监控体系、定期分析慢查询、根据业务特点优化参数配置，才能保持数据库的高性能运行。</p>`,49)])])}const E=i(h,[["render",l]]);export{c as __pageData,E as default};
