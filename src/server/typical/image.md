# 图片上传
看似简单的图片上传，实际涉及文件存储、格式转换、压缩优化、访问加速等多个环节，是典型的 IO 密集型场景。

- **文件存储**：本地存储 vs 对象存储（OSS/S3），分片上传、断点续传
- **格式处理**：格式转换（WebP/AVIF）、尺寸裁剪、水印添加、质量压缩
- **访问优化**：CDN 加速、图片懒加载、响应式图片、缩略图策略
- **安全控制**：文件类型校验、病毒扫描、访问权限、盗链防护
- **异步处理**：上传后转码队列、进度通知、失败重试